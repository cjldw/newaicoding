# R19.F6:BUG-UI-073 审计日志「详情」列「JSON 摘要」断词换行

> 状态:✅ 完成(2026-09-25 rd-fix 第 27 轮;verified 迁移 ISSUES.md)
> 模块:M9(前端 AuditLogsPage;0 后端)
> 关联:BUG-UI-073(2026-09-24 走查批次);前承 R19.F5

## 描述(BUGS.md 原文要点)

- **复现**:`/admin/audit-logs`,观察「详情」列
- **期望 vs 实际**:「JSON 摘要」链接文字被断词换行为「JSON 摘/要」两行,观感破碎(截图 10-admin-audit-logs.png 详情列);期望单行完整显示

## 实现要点

1. 详情列单元格(或链接元素)加 `white-space: nowrap`(按页面既有 Tailwind 风格 `whitespace-nowrap` 落实);必要时给该列 min-width 兜底
2. 文案保持「JSON 摘要」不变(最小修复,不做文案简化)

## 测试验证逻辑(回归判据)

| # | 判据 | 结果 |
|---|---|---|
| 1 | 代码级:详情列 nowrap 生效(类/样式断言) | ✅ TableCell whitespace-nowrap(L300)+ colgroup 详情列 90→110(L221)grep 断言 |
| 2 | 前端:tsc 0 错 + build | ✅ tsc -b exit 0+build 过(仅既有 chunk 警告) |
| 3 | 视觉:单行显示不再断词(浏览器核对归 rd-test/用户一瞥) | ⏳ 归 rd-test/用户一瞥(静态+构建已闭) |

## 决策留痕

- nowrap 优于改文案——「JSON 摘要」语义完整;详情列位于行尾,列宽自然扩展不破坏表格布局
