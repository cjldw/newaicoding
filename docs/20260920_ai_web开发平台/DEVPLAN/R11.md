# R11:在线编辑器(双模式)

> 状态:⬜ 未开始
> 模块:M5 执行环境
> 依赖:R8(任务模式)/ R2(项目模式)
> 实施轨:轨1-样板页(无设计稿;PRD「设计稿引用」=无,UI 按 DESIGN.md shadcn/ui New York 规范与项目通用组件实现)

## PRD 引用

### 边界定义

- **做什么**:
  - **任务内模式**:
    - 文件树(增删改查/重命名/拖拽)
    - Monaco(语法高亮/查找替换/多光标/minimap)
    - 文件保存即写入容器(500ms 防抖)
    - **AI 修改实时刷新**(文件 watcher + 编辑器平滑更新)
    - **任务级 Diff 视图**:列出本任务 git diff 涉及文件,逐文件 diff,可"接受/拒绝"(拒绝=回滚该文件)
    - **文件树「全部/变更」双视图**(2026-09-21 Q27):变更视图列出**本任务改动文件清单**——相对 `base_branch` 的**全部差异**(已 commit + 未 commit,git 口径,不区分 AI/人),**按仓库分组**(主仓库 / test 仓库等),每文件标注变更类型(M/A/D/R)与行数(+X/-Y);面板徽标显示变更总数;**点击文件直达其 Diff 视图**(接受/拒绝操作在 Diff 视图完成)
  - **项目模式**:
    - 通过 GitLab API 浏览任意分支的文件树与文件内容
    - 只读,不可编辑;不可创建/删除/重命名
- **不做什么**:
  - 多人实时协同(光标共享/CRDT)—— V1 假设任务**单执行者**,人审阅 AI 而非与人协同
  - LSP/智能补全/跳转定义
  - Git 可视化操作面板(stage/commit/push 走终端)
  - 二进制预览(图片除外)
  - 大文件编辑(> 2MB 只读)

### 字段定义

沿用文件树常规字段(path/type/size/mtime),无需持久化(任务模式读容器,项目模式读 GitLab API)

### 交互规则

| 场景/条件 | 行为/规则 | 结果/去向 |
|---|---|---|
| 项目模式打开文件 | 调 GitLab API `GET /projects/:id/repository/files/:path?ref={branch}` → 显示 | 只读 |
| 任务模式打开文件 | 从容器读 → 显示 | 可编辑 |
| AI 修改了打开的文件 | watcher 触发 → 编辑器更新 + toast"AI 修改了此文件" | |
| 用户编辑与 AI 修改冲突 | **以容器为准**,提示"AI 已修改,已为你重新加载" | |
| 任务结束 → Diff 视图 | 列出本任务 git diff 文件,逐文件 accept/reject | reject = `git checkout HEAD@{task_start} -- {file}` |
| 执行中 AI 修改文件(变更视图打开中) | 列表实时刷新(watcher/事件驱动),徽标 +1 | |
| 点击变更文件 | 直达该文件 Diff 视图 | 接受/拒绝在 Diff 视图 |
| 变更文件 > 500 | 列表虚拟滚动,提示"建议按仓库/目录聚焦" | 不截断 |
| 文件被外部删除 | 编辑器标记,可另存 | |

### 异常与边界场景

- 多人打开同一文件:不冲突(各自编辑,后保存覆盖前者;V1 接受)
- GitLab API 超时 → 显示"无法加载,请稍后重试"
- 文件 watcher 失效 → 手动刷新按钮

### 验收标准

1. 项目模式能从 GitLab API 浏览任意分支文件
2. 任务模式 AI 修改的文件实时刷新可见
3. Diff 视图能列出任务所有改动并可单独回滚
4. 项目模式只读,任何写操作被禁
5. 文件树变更视图列出本任务全部改动文件(类型 + 行数,按仓库分组),点击直达 Diff(Q27)

## 依赖

- R8(任务模式:容器)
- R2(项目模式:GitLab API)

## 涉及模块/文件

**后端**:
- `backend/app/api/files.py`(文件操作接口)
- `backend/app/services/file_service.py`(文件业务逻辑)
- `backend/app/services/gitlab_service.py`(GitLab API 调用,项目模式)

**Runner**:
- `runner/file_watcher.py`(文件 watcher,inotify)

**前端**:
- `frontend/src/components/FileTree.tsx`(文件树)
- `frontend/src/components/Editor.tsx`(Monaco 封装)
- `frontend/src/components/DiffViewer.tsx`(Diff 视图)
- `frontend/src/pages/tasks/TaskDetail.tsx`(任务工作台,嵌入编辑器)
- `frontend/src/pages/projects/ProjectDetail.tsx`(项目详情,代码 Tab 只读模式)

## 复用清单

**后端**:
- FastAPI `Depends(get_current_user)` 依赖注入
- `python-gitlab`(GitLab API 客户端,项目模式)
- Runner 通信(R16)

**Runner**:
- `watchdog`(Python 文件 watcher)

**前端**:
- `@monaco-editor/react`(Monaco React 封装)
- `react-diff-viewer-continued`(Diff 视图)
- shadcn/ui 组件:`Button`, `Tabs`, `Alert`
- lucide-react 图标:`FileText`, `Folder`, `Save`, `RefreshCw`, `Check`, `X`

## 实现要点

### 数据模型变更

(无需持久化,文件树/内容实时读容器或 GitLab API)

### 接口契约

**接口契约** 总览——**响应结构**统一外层 `{"code": 0, "data": ..., "message": "ok"}`(错误时 `code` 为业务错误码、`message` 为提示文案);字段级结构以下方各接口小节的请求/响应示例为准:

| 接口范围 | 响应结构约定 |
|---|---|
| 本需求点全部接口(下方小节逐一列出) | 统一外层 `{code, data, message}`;分页接口 `data` 含 `{items, total}` |

#### 1. 项目模式:文件树

- **路径**:`GET /api/projects/{project_id}/files`
- **鉴权**:JWT(项目成员)
- **请求**:
  - Query 参数:`branch`(默认 master),`path`(默认 /)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "items": [
        {
          "path": "src/main.py",
          "type": "file",
          "size": 1234,
          "mtime": "datetime"
        }
      ]
    },
    "message": "ok"
  }
  ```

#### 2. 项目模式:文件内容

- **路径**:`GET /api/projects/{project_id}/files/content`
- **鉴权**:JWT(项目成员)
- **请求**:
  - Query 参数:`branch`,`path`
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "path": "src/main.py",
      "content": "print('hello')",
      "encoding": "utf-8"
    },
    "message": "ok"
  }
  ```

#### 3. 任务模式:文件树

- **路径**:`GET /api/tasks/{task_id}/files`
- **鉴权**:JWT(项目成员)
- **请求**:
  - Query 参数:`path`(默认 /workspace/main)
- **响应**:同项目模式

#### 4. 任务模式:文件内容(读)

- **路径**:`GET /api/tasks/{task_id}/files/content`
- **鉴权**:JWT(项目成员)
- **请求**:
  - Query 参数:`path`
- **响应**:同项目模式

#### 5. 任务模式:文件内容(写)

- **路径**:`PUT /api/tasks/{task_id}/files/content`
- **鉴权**:JWT(editor 以上)
- **请求**:
  ```json
  {
    "path": "src/main.py",
    "content": "print('hello world')"
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "message": "保存成功"
  }
  ```

#### 6. 任务模式:文件操作(增删改)

- **路径**:`POST /api/tasks/{task_id}/files/operations`
- **鉴权**:JWT(editor 以上)
- **请求**:
  ```json
  {
    "operation": "create|delete|rename",
    "path": "src/main.py",
    "new_path": "src/main2.py"
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "message": "操作成功"
  }
  ```

#### 7. 任务模式:Diff 视图

- **路径**:`GET /api/tasks/{task_id}/files/diff`
- **鉴权**:JWT(项目成员)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "files": [
        {
          "path": "src/main.py",
          "status": "modified",
          "diff": "@@ -1,1 +1,1 @@\n-print('hello')\n+print('hello world')"
        }
      ]
    },
    "message": "ok"
  }
  ```

#### 8. 任务模式:变更文件清单(Q27,变更视图数据源)

- **路径**:`GET /api/tasks/{task_id}/files/changes`
- **鉴权**:JWT(项目成员)
- **请求**:无参数(base_branch 从任务记录读)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "total": 3,
      "repos": [
        {
          "repo_id": "uuid",
          "repo_role": "main",
          "files": [
            {
              "path": "src/main.py",
              "status": "M",
              "additions": 12,
              "deletions": 3
            },
            {
              "path": "src/new.py",
              "status": "A",
              "additions": 40,
              "deletions": 0
            }
          ]
        }
      ]
    },
    "message": "ok"
  }
  ```
- **status 枚举**:M=修改 / A=新增 / D=删除 / R=重命名(git name-status 原值;R 附 `old_path` 字段)
- **服务端行为**:平台经 Runner 在每个挂载仓库目录执行 `git diff --numstat --name-status {base_branch}`(工作树 vs base_branch,天然同时覆盖已 commit + 未 commit,git 口径,不区分 AI/人);重命名条目为 `R100\told => new`

#### WebSocket 文件 watcher 频道

- **路径**:`/ws/tasks/{task_id}/files`
- **鉴权**:JWT(项目成员)
- **消息协议**:
  - Runner → 平台 → 前端:
    ```json
    {"type": "file_changed", "path": "src/main.py", "content": "..."}
    {"type": "file_deleted", "path": "src/main.py"}
    ```

### 权限矩阵

| 操作 | owner | editor | viewer |
|---|---|---|---|
| 项目模式查看文件 | ✅ | ✅ | ✅ |
| 任务模式查看文件 | ✅ | ✅ | ✅ |
| 任务模式编辑文件 | ✅ | ✅ | ❌ |
| 任务模式文件操作(增删改) | ✅ | ✅ | ❌ |

### 服务端行为规格

#### 项目模式:文件树/内容

1. 平台调 GitLab API:`GET /api/v4/projects/{repo_id}/repository/tree?ref={branch}&path={path}`
2. 返回文件树

#### 任务模式:文件树/内容(读)

1. 平台通过 Runner WebSocket 发送:
   ```json
   {
     "type": "read_file",
     "container_id": "docker_id",
     "path": "/workspace/main/src/main.py"
   }
   ```
2. Runner 读容器文件 → 回报平台 → 平台返回前端

#### 任务模式:文件内容(写)

1. 平台通过 Runner WebSocket 发送:
   ```json
   {
     "type": "write_file",
     "container_id": "docker_id",
     "path": "/workspace/main/src/main.py",
     "content": "..."
   }
   ```
2. Runner 写容器文件 → 回报平台 → 平台返回前端

#### 文件 watcher

1. Runner 用 `watchdog` 监听容器内 `/workspace` 目录
2. 文件变更 → Runner 发送:
   ```json
   {
     "type": "file_changed",
     "container_id": "docker_id",
     "path": "/workspace/main/src/main.py"
   }
   ```
3. 平台转发到前端(通过 `/ws/tasks/{tid}/files` 频道)

### 页面结构说明

#### 任务工作台编辑器区

- **区块结构**:
  1. **左侧文件树**(双视图,Q27):顶部 `Tabs`(「全部文件」/「变更文件」+ 徽标显示变更总数)
     - 全部文件 Tab:树形展示,支持增删改查/重命名/拖拽
     - 变更文件 Tab:按仓库分组的扁平列表,每行 = 变更类型徽标(M/A/D/R)+ 文件路径 + 行数标注(`+X/-Y`);> 500 行虚拟滚动
  2. **右侧编辑器**:Monaco,多 Tab
  3. **顶部 Tab 栏**:已打开的文件
  4. **底部状态栏**:当前文件路径 / 编码 / 行列号

#### 项目详情页代码 Tab(只读模式)

- **区块结构**:
  1. **左侧文件树**:树形展示,只读
  2. **右侧编辑器**:Monaco,只读
  3. **顶部分支选择器**:Select(默认 master)

### 页面交互流程

#### 任务模式:打开文件流程

1. 用户在文件树点击文件 → 前端调 `GET /api/tasks/{tid}/files/content?path=...`
2. 成功后 Monaco 显示文件内容
3. 用户编辑 → 500ms 防抖 → 调 `PUT /api/tasks/{tid}/files/content`
4. AI 修改了打开的文件 → 文件 watcher 触发 → 前端收到 `file_changed` 消息 → 编辑器平滑更新 + toast"AI 修改了此文件"

#### 任务模式:Diff 视图流程

1. 任务结束后,用户点"查看 Diff"
2. 前端调 `GET /api/tasks/{tid}/files/diff`
3. 显示 Diff 视图(逐文件 diff)
4. 用户点"接受" → 无操作(已 commit)
5. 用户点"拒绝" → 调 `POST /api/tasks/{tid}/files/operations`(operation=revert,path=...)→ Runner 在容器内执行 `git checkout HEAD@{task_start} -- {file}`

#### 任务模式:变更视图流程(Q27)

1. 用户在文件树面板点「变更文件」Tab → 前端调 `GET /api/tasks/{tid}/files/changes`
2. 按仓库分组渲染列表;面板 Tab 徽标显示 `total`
3. 执行中收到 `file_changed`/`file_deleted` watcher 消息 → 防抖 500ms 重新拉取清单(或本地增量合并)→ 徽标与列表实时刷新
4. 点击列表行 → 该文件在右侧以 Diff 视图打开(调 `GET /api/tasks/{tid}/files/diff` 过滤该文件);接受/拒绝操作在 Diff 视图完成,操作后清单刷新(该文件行数与状态更新)
5. 变更文件 > 500 → 列表虚拟滚动 + 顶部提示"建议按仓库/目录聚焦"

### 控件联动

| 触发控件 | 事件 | 响应行为 |
|---|---|---|
| 文件树「全部文件/变更文件」Tabs | 切换 | 全部=树形;变更=按仓库分组清单(调 changes 接口) |
| 文件树节点 | 点击 | 打开文件 |
| 变更文件列表行 | 点击 | 右侧以 Diff 视图打开该文件 |
| 文件树节点 | 右键 | 显示上下文菜单(新建/重命名/删除) |
| Monaco 编辑器 | 编辑 | 500ms 防抖自动保存 |
| Monaco 编辑器 | Ctrl+S | 立即保存 |

### 文案清单

| 场景 | 文案 |
|---|---|
| AI 修改文件 toast | AI 修改了此文件 |
| 文件被外部删除提示 | 文件已被外部删除 |
| 保存成功提示 | 保存成功 |
| 项目模式只读提示 | 只读模式,不可编辑 |
| 查看 Diff 按钮 | 查看 Diff |
| 接受按钮 | 接受 |
| 拒绝按钮 | 拒绝 |
| 文件树 Tab-全部 | 全部文件 |
| 文件树 Tab-变更 | 变更文件 |
| 变更多提示 | 建议按仓库/目录聚焦 |
| 变更类型徽标 | M / A / D / R |

### 设计规范

**Token**:同 R1

**布局**:
- 任务工作台编辑器区:左侧文件树(250px)+ 右侧编辑器(自适应)
- 项目详情页代码 Tab:同任务工作台

**组件**:
- Tabs:shadcn/ui `Tabs` 组件
- Monaco:`@monaco-editor/react`(深色主题 vs-dark)
- DiffViewer:`react-diff-viewer-continued`

**状态**:
- 按钮:同 R1
- 文件树节点:hover 时 `bg-surface-strong`

**效果**:
- 过渡:同 R1

### 渲染核对期望

**结构**:
- 文件树:宽度 250px,固定
- 编辑器:自适应宽度

**Token**:
- Monaco 主题:vs-dark
- 文件树字体:`--font-sans`

**状态**:同 R1

**效果**:同 R1

### 设计还原风险清单

(无设计稿,按 DESIGN.md + shadcn/ui New York 规范实现;Monaco 用默认深色主题)

## 完成判据

### 功能

1. ✅ 项目模式能从 GitLab API 浏览任意分支文件
2. ✅ 项目模式只读,任何写操作被禁
3. ✅ 任务模式能编辑文件(500ms 防抖自动保存)
4. ✅ AI 修改的文件实时刷新可见(文件 watcher)
5. ✅ Diff 视图能列出任务所有改动并可单独回滚
6. ✅ 文件树支持增删改查/重命名/拖拽
7. ✅ 变更视图:相对 base_branch 全部差异(已 commit + 未 commit),按仓库分组,类型/行数标注,徽标总数,点击直达 Diff(Q27)
8. ✅ 变更文件 > 500 虚拟滚动不截断(Q27)

### 异常场景

1. ✅ GitLab API 超时:显示"无法加载,请稍后重试"
2. ✅ 文件 watcher 失效:手动刷新按钮
3. ✅ 多人打开同一文件:后保存覆盖前者(V1 接受)

### UI

1. ✅ 文件树:树形展示,支持增删改查/重命名/拖拽
2. ✅ 编辑器:Monaco 深色主题,多 Tab
3. ✅ Diff 视图:逐文件 diff,可接受/拒绝
4. ✅ 所有文案符合"文案清单"

## 测试验证逻辑

### 单元测试(后端)

- `test_project_mode_file_tree`:项目模式文件树(模拟 GitLab API)
- `test_task_mode_read_file`:任务模式读文件(模拟 Runner)
- `test_task_mode_write_file`:任务模式写文件
- `test_file_watcher_event`:文件 watcher 事件推送到前端
- `test_task_mode_changes_list`:变更清单返回相对 base_branch 全部差异(含未 commit)(Q27)
- `test_task_mode_changes_grouped_by_repo`:变更清单按仓库分组,total 正确(Q27)

### 单元测试(Runner)

- `test_file_watcher`:Runner 能监听文件变更

### 集成测试(前端)

- 打开文件 → 编辑 → 自动保存
- AI 修改文件 → 编辑器实时刷新
- 查看 Diff → 拒绝某个文件 → 文件回滚
- 切「变更文件」Tab → 列表按仓库分组显示,徽标计数正确;点行直达 Diff;AI 改动时列表实时刷新(Q27)

### 手动测试

- 在容器里用 AI 修改文件,验证编辑器实时刷新
- 在编辑器里手动修改文件,验证自动保存
- 任务结束后查看 Diff,验证能接受/拒绝

## 实现进度

(rd-dev 维护)

- [ ] 接口(项目模式文件树/内容,任务模式文件树/内容/操作/Diff/变更清单)
- [ ] Runner 文件 watcher
- [ ] WebSocket 文件 watcher 频道
- [ ] 前端 FileTree 组件(含「全部/变更」双视图,Q27)
- [ ] 前端 Editor 组件(Monaco 封装)
- [ ] 前端 DiffViewer 组件
- [ ] 单元测试
- [ ] 集成测试
- [ ] 手动测试
