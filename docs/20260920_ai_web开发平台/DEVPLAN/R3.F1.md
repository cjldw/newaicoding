# R3.F1 — 修复 BUG-035:「开始打磨需求」→ 创建任务 + 初始化容器(验证优先)

> 来源:BUGS.md BUG-035(用户指令 2026-09-23 rd-fix 第 15 轮)
> 状态:✅ fixed(2026-09-23,E2E 推翻静态结论:create_polish_task 缺 Task 落库,已补;容器回填实证 cid=50f386fa63b4)

## 描述(即 bug)

用户要求:需求创建后,点击「开始打磨需求」= 创建一个任务 + 初始化启动容器。历史 BUG-023 曾实测"draft 需求详情页点击开始打磨按钮页面无变化"。

## 静态分析结论(2026-09-23,底稿 `.scratch/fix-analysis-r15d-polish.md`)

**代码链路已完整存在**(静态评估零改动):
- 前端 `RequirementDetail.tsx` L104-114/L196-201:按钮(draft 态)→ `handlePolish()` → `POST /requirements/{reqId}/polish` → 成功 `navigate(/tasks/{task_id})`;`disabled={isPending}` 防重
- 后端 `api/requirements.py` L143-156 `/polish` 端点(editor+)→ `requirement_service.start_polish()` L152-178(draft 校验 + polish_task_id 幂等 3001 + status→polishing)
- `task_service.create_polish_task()` L63-115:type=requirement(Enum 已含)任务 + env 注入(LLM/ANTHROPIC/GITLAB/TASK_ID/REQ_ID/PRD_FILE_PATH)→ **`container_service.schedule_and_start()` 直调拉起容器**
- `TaskDetail.tsx` L76/L212/L534:requirement 类型(打磨/Sparkles/PRD 面板)已支持

**静态结论不可轻信的理由**:BUG-023 实测曾"点击无变化"(与静态分析矛盾);且相关文件处于多轮未提交工作树状态。**本分片以真实 E2E 回归为一等判据,有缺口才修码。**

## 实现要点(方案)

1. **E2E 实证(先行)**:真实后端(127.0.0.1:8000)+ 本地 Runner 在线环境:
   - 创建 draft 需求 → `POST /requirements/{req_id}/polish`(editor token)→ 断言 200 + `data.task_id`
   - 断言 tasks 行:`type="requirement"`、`req_id` 关联、需求 `status="polishing"`、`polish_task_id` 回填
   - 断言容器链:schedule_and_start 触发(container_started 回报后 `tasks.container_id/runner_id` 回填,参照 BUG-030 判据)
   - 幂等:二次 POST → 3001
   - UI:需求详情页点击按钮 → 跳转 `/tasks/{taskId}`,工作台显示"打磨"类型
2. **缺口修复(仅当 E2E 失败)**:按失败环节定位(按钮 onClick/API 404/service 校验/容器调度失败),最小改动修复;不重构既有链路
3. 常见前置坑位排查:需求需属真实项目且操作者为 editor+;Runner 必须在线否则 8003(属环境而非代码缺陷,如实记录)

## 测试验证逻辑(修复后按此回归)

1. pytest:打磨链路若无既有用例,补 API 级用例(draft→polish→3001 幂等→状态流转;容器调度 mock 断言 schedule_and_start 被调),进全量回归
2. E2E 实证判据同上(任务行 + 需求行 + 容器回填 + UI 跳转),证据落 `.scratch/R3.F1/e2e.md`
3. 后端无改动时以 E2E + 既有 pytest 全绿为通过判据;有改动则全量 pytest 零回退


## 实现进度(2026-09-23)

- [x] E2E 实证(判据 1-7 全过,证据 `.scratch/R3.F1/e2e.md`)
- [x] 缺口修复:E2E 抓出 create_polish_task **缺 Task 行落库**(静态分析误判"链路完整"),已补先 INSERT 再调度、成功置 running;调度失败整体回滚
- [x] 回归:requirements/projects/dashboard/r8f4 子集 59 passed 1 skipped;全量见 DEVPLAN 变更记录
