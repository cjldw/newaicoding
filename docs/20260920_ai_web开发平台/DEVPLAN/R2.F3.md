# R2.F3 守卫:项目管理页保留右上角「新建项目」按钮(BUG-UI-005)

> 状态:⬜(运行时核实:按钮存在且与 vp 一致 → 本分片不改码,仅承载回归断言防丢失)

## 描述(bug)

BUG-UI-005(用户指令:"项目管理保留右上角的添加按钮")。

**运行时核实(2026-09-22 Playwright,截图 recon-projects.png)**:/projects 页右上角「新建项目」按钮**存在且与 vp 一致**——
- 位置:`.page-head > .acts` ✅
- 样式:`btn btn-pri` ✅
- 内容:Plus 图标 + 「新建项目」✅
- 行为:跳转 /projects/create(实际功能,vp 为演示 toast)✅

**判定:误报/守卫型指令**——用户要求"保留",当前实现已满足;登记本分片是为了把该要求固化为回归断言,防止后续 UI 迭代再丢失。

## 实现要点

- **不改业务代码**。
- 可选健壮性微调(仅当修复 R2.F2/R19.F4 时顺手):`<Link to="/projects/create"><button>` 嵌套改为 `<Link className="btn btn-pri">`(HTML 交互元素嵌套更稳),行为不变。**不做强求**。

## 测试验证逻辑(回归断言)

Playwright 断言 /projects(超管登录):
1. `.page-head .acts .btn-pri` 存在且可见,文本含「新建项目」
2. 按钮右对齐于 `.page-head`(acts 右缘 ≈ page-head 右缘,误差 ≤ 2px)
3. 点击后跳转 /projects/create(或打开创建流程)

## 实施轨 + 对应设计稿(继承自 R2 母分片)

- 轨1:vp 原型 pageProjects(vp L862-885)

## 路由与入口

- /projects

## 渲染核对期望(修复重点:BUG-UI-005 保留右上角添加按钮)

| 元素 | 属性 | 期望值 |
|---|---|---|
| .acts .btn-pri | 存在性/文本 | 可见,「新建项目」+ plus 图标 |
| .acts | 对齐 | 右上角(与 page-head 右缘对齐) |

## 设计规范(仅修复涉及块)

- `.btn-pri` / `.page-head` / `.acts` 均用 globals.css 既有移植值,不新增样式
