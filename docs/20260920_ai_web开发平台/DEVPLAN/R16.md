# R16:Runner 管理(分布式容器执行)

> 状态:✅ 已完成(2026-09-22;测试 175 全绿 / build 通过 / 主 agent 直审通过,详见 .scratch/R16/)
> 模块:M5 执行环境
> 依赖:R1(超管)
> 实施轨:轨1-样板页(无设计稿;PRD「设计稿引用」=无,UI 按 DESIGN.md shadcn/ui New York 规范与项目通用组件实现)

## PRD 引用

### 边界定义

- **做什么**:
  - **Runner 注册**:运维在某台机器上 `docker run -e PLATFORM_URL=... -e RUNNER_TOKEN=... platform/runner:v1` → Runner 启动后**主动 WebSocket 连接平台** → 平台校验 token → 注册上线
  - **任务调度**:平台后端调度器根据"最少负载"策略选择 Runner(部署任务固定在 `role=deploy` 的 Runner)
  - **容器管理**:平台通过 WebSocket 向 Runner 发送指令(启动/停止/销毁/exec);Runner 本地调 Docker SDK 执行,并回报结果
  - **心跳保活**:Runner 每 30s 向平台发心跳;60s 无心跳平台标记 Runner offline
  - **状态回传**:Runner 监听本地容器事件(start/die/OOM 等),实时回传平台
  - **端口映射回报**:Runner 启动容器时分配随机宿主机端口,回报给平台(网关直连用,按 ARCH D20/PRD Q25:**取消 Runner 本地代理层**——平台网关直接转发到 `runner_host:mapped_port`,Runner 机器不再部署 Nginx/Traefik 代理)
  - **Runner 角色**:`worker`(默认,跑任务容器)/ `deploy`(专用跑部署容器,需绑定公网 IP/域名)
- **不做什么**:
  - Runner 自动扩缩容(V1 手动增减 Runner)
  - Runner 上的容器迁移(Runner 掉线,容器继续跑;任务不迁移到其他 Runner)
  - Runner 标签匹配(V1 只区分 worker/deploy;V2 再加 GPU/大内存等标签)
  - Kubernetes(Runner 就是"装了 Docker 的机器",V1 不上 K8s)

### 字段定义

(Runner 表)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| runner_id | uuid | 是 | auto | |
| name | string(64) | 是 | - | 运维起的名字(如 "runner-beijing-01") |
| role | enum | 是 | worker | worker / deploy |
| token_hash | string | 是 | - | Runner token 的 bcrypt hash(平台不存明文) |
| status | enum | 是 | offline | online / offline / disabled |
| last_heartbeat_at | datetime | 否 | null | |
| machine_info | json | 否 | null | `{os, arch, cpu_count, mem_total_gb, docker_version}` |
| current_containers | int | 是 | 0 | 当前运行的容器数(调度用) |
| max_containers | int | 是 | 10 | 该 Runner 最多跑多少容器 |
| public_ip | string(64) | 否 | null | deploy Runner 必填(部署 URL 指向该 IP) |
| created_by | ref(R1) | 是 | - | 创建 token 的超管 |
| created_at | datetime | 是 | now | |

### 交互规则

| 场景/条件 | 行为/规则 | 结果/去向 |
|---|---|---|
| 超管生成 Runner token | 平台设置 → Runner 管理 → "新建 Runner" → 填 name + role → 生成一次性 token(仅显示一次) | 运维复制 token |
| Runner 启动 | `docker run -e PLATFORM_URL=wss://platform.example.com/ws/runner -e RUNNER_TOKEN=xxx platform/runner:v1` → 连接平台 → 平台校验 token_hash → 标记 online | Runner 列表显示 online |
| 心跳 | Runner 每 30s 发 `ping`;平台 60s 未收到 → 标记 offline | Runner 列表灰显 |
| 任务调度 | 平台选 `status=online AND current_containers < max_containers AND role 匹配` 的 Runner 中 `current_containers` 最少的 | 任务分配到该 Runner |
| Runner 上的容器事件 | Runner 监听 docker events → start/die/OOM → 实时回传平台 → 平台更新容器状态 | 前端可见 |
| Runner 下线(运维主动) | Runner 收到 SIGTERM → 通知平台 → 平台标记 offline → Runner 上的 running 容器**不动**(继续跑) | Runner 列表灰显 |
| Runner 恢复 | Runner 重新连接 → 上报本地实际运行的容器列表 → 平台对账(数据库 vs 实际) | 状态一致 |
| 部署任务调度 | 仅选 `role=deploy AND online` 的 Runner;多个 deploy Runner 时选最少负载 | 部署固定在 deploy Runner |
| Runner token 泄露 | 超管在 Runner 管理页"重置 token" → 旧 token 失效,Runner 需用新 token 重启 | 安全 |

### 异常与边界场景

- **Runner offline 时其上的容器**:继续跑(Runner 是薄代理,容器独立于 Runner 进程);平台标记容器"Runner offline",**WebSocket 终端/预览不可用**(网关无法转发);Runner 恢复后恢复
- **Runner 全部 offline**:任务创建时**排队**,前端显示"等待可用 Runner";超管告警
- **Runner 上 Docker daemon 挂了**:Runner 上报 "docker daemon unavailable" → 平台标记 Runner failed,不再调度
- **Runner 时钟漂移**:心跳带时间戳,平台校验时钟差 > 5min 拒绝(避免 token 重放)
- **Runner 与平台时钟不同步导致的心跳误判**:平台容忍 ±30s 时钟差

### 验收标准

1. 运维能用 token 在某台机器上启动 Runner 并注册到平台
2. 任务能分配到 Runner 并在其上拉起容器
3. Runner offline 时任务排队,Runner 恢复后继续
4. Runner 上的容器事件(start/die)能实时回传平台
5. 部署任务固定在 deploy Runner 上
6. Runner token 重置后旧 token 失效

## 依赖

R1(超管)

## 涉及模块/文件

**后端**:
- `backend/app/models/runner.py`(Runner 模型)
- `backend/app/schemas/runner.py`(Pydantic 模型)
- `backend/app/api/admin/runners.py`(Runner 管理接口,超管)
- `backend/app/services/runner_service.py`(Runner 业务逻辑)
- `backend/app/core/websocket.py`(WebSocket 网关,Runner 频道 `/ws/runner`)

**Runner**:
- `runner/main.py`(Runner 主进程,WebSocket 客户端)
- `runner/auth.py`(token 校验)
- `runner/heartbeat.py`(心跳保活)
- `runner/container_manager.py`(容器管理,调 Docker SDK;端口直接映射宿主机,无本地代理层,按 D20)

**前端**:
- `frontend/src/pages/admin/RunnerManagement.tsx`(Runner 管理页,超管)
- `frontend/src/api/admin/runners.ts`(API 客户端)

## 复用清单

**后端**:
- FastAPI `Depends(get_current_user)` 依赖注入(超管校验)
- SQLAlchemy 2.0 async session
- `passlib[bcrypt]`(Runner token 哈希)
- WebSocket 网关(D5)

**Runner**:
- `websockets`(Python WebSocket 客户端)
- `docker`(Python Docker SDK)
- `asyncio`

**前端**:
- shadcn/ui 组件:`Button`, `Input`, `Card`, `Table`, `Dialog`, `Select`, `Badge`, `Alert`
- react-query `useQuery` / `useMutation`
- lucide-react 图标:`Plus`, `Trash`, `RefreshCw`, `Server`, `Check`, `X`

## 实现要点

### 数据模型变更

**新增表 `runners`**:

| 字段 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | BIGINT UNSIGNED | 是 | AUTO_INCREMENT | PRIMARY KEY | 内部 id |
| runner_id | CHAR(36) | 是 | UUID | UNIQUE | 对外 id(uuid) |
| name | VARCHAR(64) | 是 | - | UNIQUE | Runner 名 |
| role | ENUM('worker', 'deploy') | 是 | 'worker' | INDEX | 角色 |
| token_hash | VARCHAR(255) | 是 | - | - | bcrypt 哈希 |
| status | ENUM('online', 'offline', 'disabled') | 是 | 'offline' | INDEX | 状态 |
| last_heartbeat_at | DATETIME | 否 | NULL | - | 最后心跳时间 |
| machine_info | JSON | 否 | NULL | - | 机器信息 |
| current_containers | INT | 是 | 0 | - | 当前运行的容器数 |
| max_containers | INT | 是 | 10 | - | 最多跑多少容器 |
| public_ip | VARCHAR(64) | 否 | NULL | - | deploy Runner 的公网 IP |
| created_by | CHAR(36) | 是 | - | - | 创建者 user_id(超管) |
| created_at | DATETIME | 是 | CURRENT_TIMESTAMP | - | 创建时间 |
| updated_at | DATETIME | 是 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | - | 更新时间 |

**SQL 全文见 `DEPLOY.md`**

### 接口契约

**接口契约** 总览——**响应结构**统一外层 `{"code": 0, "data": ..., "message": "ok"}`(错误时 `code` 为业务错误码、`message` 为提示文案);字段级结构以下方各接口小节的请求/响应示例为准:

| 接口范围 | 响应结构约定 |
|---|---|
| 本需求点全部接口(下方小节逐一列出) | 统一外层 `{code, data, message}`;分页接口 `data` 含 `{items, total}` |

#### 1. Runner 列表(超管)

- **路径**:`GET /api/admin/runners`
- **鉴权**:JWT(超管)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "items": [
        {
          "runner_id": "uuid",
          "name": "runner-beijing-01",
          "role": "worker",
          "status": "online",
          "last_heartbeat_at": "datetime",
          "machine_info": {
            "os": "linux",
            "arch": "x86_64",
            "cpu_count": 8,
            "mem_total_gb": 32,
            "docker_version": "24.0.5"
          },
          "current_containers": 3,
          "max_containers": 10,
          "public_ip": null,
          "created_at": "datetime"
        }
      ]
    },
    "message": "ok"
  }
  ```

#### 2. 创建 Runner(生成 token)

- **路径**:`POST /api/admin/runners`
- **鉴权**:JWT(超管)
- **请求**:
  ```json
  {
    "name": "runner-beijing-01",
    "role": "worker",
    "max_containers": 10,
    "public_ip": "string(deploy 时必填)"
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "runner_id": "uuid",
      "name": "runner-beijing-01",
      "token": "plt-runner-xxx(仅显示一次)"
    },
    "message": "创建成功,请保存 token(仅显示一次)"
  }
  ```

#### 3. 重置 Runner token

- **路径**:`POST /api/admin/runners/{runner_id}/reset-token`
- **鉴权**:JWT(超管)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "token": "plt-runner-yyy(仅显示一次)"
    },
    "message": "token 已重置,旧 token 已失效"
  }
  ```

#### 4. 禁用 Runner

- **路径**:`POST /api/admin/runners/{runner_id}/disable`
- **鉴权**:JWT(超管)
- **响应**:
  ```json
  {
    "code": 0,
    "message": "Runner 已禁用"
  }
  ```

#### 5. 删除 Runner

- **路径**:`DELETE /api/admin/runners/{runner_id}`
- **鉴权**:JWT(超管)
- **响应**:
  ```json
  {
    "code": 0,
    "message": "Runner 已删除"
  }
  ```
- **错误码**:
  - `16001`: Runner 上有运行中的容器,不可删除

### 权限矩阵

| 操作 | 超管 | 普通用户 |
|---|---|---|
| 查看 Runner 列表 | ✅ | ❌ |
| 创建 Runner | ✅ | ❌ |
| 重置 token | ✅ | ❌ |
| 禁用 Runner | ✅ | ❌ |
| 删除 Runner | ✅ | ❌ |

### 服务端行为规格

#### Runner 注册(Runner → 平台)

1. Runner 启动后 WebSocket 连接 `wss://platform.example.com/ws/runner`
2. 发送注册消息:
   ```json
   {
     "type": "register",
     "token": "plt-runner-xxx",
     "machine_info": {
       "os": "linux",
       "arch": "x86_64",
       "cpu_count": 8,
       "mem_total_gb": 32,
       "docker_version": "24.0.5"
     }
   }
   ```
3. 平台校验 token(bcrypt hash 对比 `runners.token_hash`)
4. 校验通过 → 更新 `runners` 表(status=online, last_heartbeat_at=now(), machine_info)
5. 返回:
   ```json
   {
     "type": "register_success",
     "runner_id": "uuid"
   }
   ```

#### 心跳保活(Runner → 平台)

1. Runner 每 30s 发送:
   ```json
   {
     "type": "heartbeat",
     "timestamp": 1234567890
   }
   ```
2. 平台收到后更新 `last_heartbeat_at`
3. 平台定时任务(每 60s)检查 `last_heartbeat_at`,超过 60s 未更新 → 标记 status=offline

#### 任务调度(平台)

1. 任务创建时,平台调度器查询:
   ```sql
   SELECT * FROM runners
   WHERE status='online'
     AND current_containers < max_containers
     AND role = 'worker'  -- 或 'deploy'(部署任务)
   ORDER BY current_containers ASC
   LIMIT 1
   ```
2. 选中 Runner 后,通过 WebSocket 发送 `start_container` 消息
3. Runner 拉起容器后回报 `container_started`,平台更新 `containers` 表 + `runners.current_containers + 1`

#### Runner 恢复(Runner → 平台)

1. Runner 重新连接后,发送:
   ```json
   {
     "type": "sync",
     "containers": [
       {"container_id": "docker_id_1", "status": "running"},
       {"container_id": "docker_id_2", "status": "stopped"}
     ]
   }
   ```
2. 平台对账:
   - 数据库里有但 Runner 上报没有 → 标记 destroyed
   - Runner 上报有但数据库里没有 → 新增(异常情况,理论上不应发生)
   - 状态不一致 → 以 Runner 上报为准

### 页面结构说明

#### Runner 管理页 `/admin/runners`

- **区块结构**:
  1. **页面标题**:"Runner 管理"
  2. **操作栏**:右侧"新建 Runner"按钮(primary)
  3. **Runner 列表**(Table):
     - 列:名称 / 角色徽章(worker=secondary/deploy=primary)/ 状态徽章(online=success/offline=secondary/disabled=outline)/ 当前容器数(3/10)/ 机器信息(CPU 8 核 / 内存 32GB)/ 最后心跳 / 操作(重置 token/禁用/删除)
     - offline 行灰显
  4. **新建 Runner 对话框**:
     - 名称输入框(label="名称",placeholder="如:runner-beijing-01")
     - 角色选择(label="角色",Select:worker/deploy)
     - 最大容器数输入框(label="最大容器数",type=number,default=10)
     - (deploy 时显示)公网 IP 输入框(label="公网 IP",placeholder="部署 URL 将指向该 IP")
     - "取消" + "创建"按钮
  5. **创建成功对话框**:
     - 提示:"Runner 创建成功,请保存 token(仅显示一次)"
     - token 显示(等宽字体,可复制)
     - 启动命令示例:
       ```bash
       docker run -d \
         -e PLATFORM_URL=wss://platform.example.com/ws/runner \
         -e RUNNER_TOKEN=plt-runner-xxx \
         -v /var/run/docker.sock:/var/run/docker.sock \
         --name runner-beijing-01 \
         platform/runner:v1
       ```
     - "关闭"按钮

### 页面交互流程

#### 创建 Runner 流程

1. 超管点"新建 Runner" → 打开对话框
2. 填表 → 点"创建"
3. 调 `POST /api/admin/runners`
4. 成功 → 显示 token + 启动命令 → 超管复制 token 给运维
5. 运维在 Runner 机器上执行启动命令 → Runner 注册到平台 → 列表显示 online

#### 重置 token 流程

1. 超管在列表点"重置 token"
2. 弹出确认对话框:"重置后旧 token 将失效,Runner 需用新 token 重启"
3. 点"确定" → 调 `POST /reset-token`
4. 成功 → 显示新 token

### 控件联动

| 触发控件 | 事件 | 响应行为 |
|---|---|---|
| 角色选择 Select | 切换 | deploy 时显示公网 IP 输入框 |
| token 显示框 | 点击 | 复制到剪贴板 |

### 文案清单

| 场景 | 文案 |
|---|---|
| Runner 管理页标题 | Runner 管理 |
| 新建 Runner 按钮 | 新建 Runner |
| 新建 Runner 对话框标题 | 新建 Runner |
| 名称 label | 名称 |
| 角色 label | 角色 |
| 角色选项-worker | 工作节点(跑任务容器) |
| 角色选项-deploy | 部署节点(跑部署容器,需公网 IP) |
| 最大容器数 label | 最大容器数 |
| 公网 IP label | 公网 IP |
| 创建按钮 | 创建 |
| 创建成功对话框标题 | Runner 创建成功 |
| token 提示 | 请保存 token(仅显示一次) |
| 启动命令示例 | 启动命令示例 |
| 关闭按钮 | 关闭 |
| Runner 列表列-名称 | 名称 |
| Runner 列表列-角色 | 角色 |
| Runner 列表列-状态 | 状态 |
| Runner 列表列-当前容器数 | 当前容器数 |
| Runner 列表列-机器信息 | 机器信息 |
| Runner 列表列-最后心跳 | 最后心跳 |
| Runner 列表列-操作 | 操作 |
| 角色徽章-worker | 工作节点 |
| 角色徽章-deploy | 部署节点 |
| 状态徽章-online | 在线 |
| 状态徽章-offline | 离线 |
| 状态徽章-disabled | 禁用 |
| 重置 token 按钮 | 重置 token |
| 禁用按钮 | 禁用 |
| 删除按钮 | 删除 |
| 重置 token 确认对话框 | 重置后旧 token 将失效,Runner 需用新 token 重启 |
| 确定按钮 | 确定 |
| 取消按钮 | 取消 |
| 错误提示-Runner 上有容器 | Runner 上有运行中的容器,不可删除 |
| 成功提示-创建成功 | Runner 创建成功 |
| 成功提示-token 已重置 | token 已重置,旧 token 已失效 |
| 成功提示-禁用成功 | Runner 已禁用 |
| 成功提示-删除成功 | Runner 已删除 |

### 设计规范

**Token**:同 R1

**布局**:
- Runner 管理页:页面标题 + 操作栏(右侧按钮)+ Table

**组件**:
- Table:同 R2
- Dialog:同 R2
- Badge:worker=secondary/deploy=primary/online=success/offline=secondary/disabled=outline

**状态**:
- 按钮/输入框/Table 行:同 R2
- offline 行:灰显(`opacity-50`)

**效果**:
- 过渡:同 R2
- Dialog:同 R2

### 渲染核对期望

**结构**:
- Runner 列表:Table 列宽自适应,操作列固定宽度 200px
- 对话框:宽度 500px,居中
- 创建成功对话框:宽度 600px,居中

**Token**:同 R2

**状态**:同 R2

**效果**:同 R2

### 设计还原风险清单

(无设计稿,按 DESIGN.md + shadcn/ui New York 规范实现)

## 完成判据

### 功能

1. ✅ 超管能创建 Runner(生成 token)
2. ✅ 运维能用 token 在某台机器上启动 Runner 并注册到平台
3. ✅ Runner 每 30s 发心跳,平台 60s 未收到标记 offline
4. ✅ 任务能分配到 Runner(最少负载 + role 匹配)
5. ✅ Runner 上的容器事件(start/die)能实时回传平台
6. ✅ 部署任务固定在 deploy Runner 上
7. ✅ 超管能重置 Runner token(旧 token 失效)
8. ✅ 超管能禁用 Runner(不再调度新任务)
9. ✅ 超管能删除 Runner(无运行中容器时)

### 异常场景

1. ✅ Runner offline 时其上的容器继续跑,Runner 恢复后状态同步正确
2. ✅ Runner 全部 offline 时任务排队
3. ✅ Runner 上 Docker daemon 挂了,平台标记 Runner failed
4. ✅ Runner 时钟漂移 > 5min,平台拒绝心跳
5. ✅ Runner 上有运行中容器时,删除失败返回 `16001`

### UI

1. ✅ Runner 列表:Table 展示名称/角色/状态/当前容器数/机器信息/最后心跳/操作
2. ✅ offline 行灰显
3. ✅ 新建 Runner 对话框:表单校验 + deploy 时显示公网 IP 输入框
4. ✅ 创建成功对话框:显示 token + 启动命令示例
5. ✅ 所有文案符合"文案清单"

## 测试验证逻辑

### 单元测试(后端)

- `test_create_runner_success`:创建 Runner 成功(生成 token)
- `test_runner_register_success`:Runner 注册成功(模拟 WebSocket 消息)
- `test_runner_heartbeat`:心跳更新 last_heartbeat_at
- `test_runner_offline_detection`:60s 未心跳标记 offline
- `test_schedule_least_loaded`:调度器选最少负载 Runner
- `test_schedule_deploy_runner`:部署任务选 deploy Runner
- `test_reset_token_success`:重置 token 成功
- `test_delete_runner_with_containers`:Runner 上有容器时删除失败返回 16001

### 单元测试(Runner)

- `test_runner_register`:Runner 能注册到平台
- `test_runner_heartbeat`:Runner 能定时发心跳
- `test_runner_sync`:Runner 恢复后能同步容器状态

### 集成测试(前端)

- 创建 Runner 流程:填表 → 点创建 → 显示 token
- 重置 token 流程:点重置 → 确认 → 显示新 token

### 手动测试

- 在 Runner 机器上手动启动 Runner,验证能注册到平台
- 创建任务,验证能分配到 Runner
- 手动停止 Runner,验证平台标记 offline
- 重启 Runner,验证状态同步正确

## 实现进度

(rd-dev 维护;2026-09-22 完成)

- [x] 数据模型(runners 表 + alembic a9c1e5f7b2d4)
- [x] 接口(GET 列表/POST 创建[token 一次性]/reset-token/disable/DELETE[16001],超管)
- [x] WebSocket 网关升级(register token+machine_info → register_success;heartbeat 时间戳漂移校验[±30s 容忍/>5min 拒绝];sync 恢复对账[以 Runner 上报为准];docker_daemon_unavailable → offline)
- [x] Runner 主进程升级(register/heartbeat 时间戳/本地容器对账上报/machine_info 采集/docker daemon 异常上报)
- [x] 平台调度器切 DB(pick_runner_db:online + current<max + role 匹配 + 最少负载前 5 随机;R8 内存版退役)
- [x] 心跳巡检(main.py lifespan 每 60s sweep,超时 → offline)
- [x] 前端(RunnerManagement 页 + api/admin/runners.ts + 新建/token 展示/重置/禁用对话框 + 侧栏超管入口;build 通过)
- [x] 单元测试(R16 11 条 + R8 平台侧改 DB 调度 10 条,全量 175 passed)
- [ ] 集成/手动测试(真实机器部署 Runner——归 R4/R15 联调)

**决策留痕**:① R8 的共享 RUNNER_TOKEN 废弃(DEPLOY 记录配置变更),改 per-runner token bcrypt;② 注册 token 无 runner_id 内嵌 → 遍历 bcrypt 比对(Runner 数量小);③ 状态枚举无 failed:docker daemon 异常按 offline 处理 + 告警日志;④ 重置/禁用/删除时主动断开 WS 连接;⑤ runner/auth.py 未单独建文件(token 透传注册消息,逻辑在平台 handle_register)
