# R17:MCP server 与 Skills 管理

> 状态:⬜ 未开始
> 模块:M3 AI 能力
> 依赖:R2
> 实施轨:轨1-样板页(无设计稿;PRD「设计稿引用」=无,UI 按 DESIGN.md shadcn/ui New York 规范与项目通用组件实现)

## PRD 引用

### 边界定义

- **做什么**:
  - **镜像预装**(platform/devbox:v1):
    - 常用 MCP server(filesystem / git / github / sqlite / brave-search)
    - 常用 Skills(平台官方维护,如"代码审查"、"写测试"、"重构")
  - **项目级 MCP server 配置**:
    - 项目设置里允许用户**编辑 JSON 配置**(沿用 Claude Code 原生格式)
    - 提供**常用 MCP server 模板**(PostgreSQL / Redis / GitHub / Slack 等),用户填参数即可
    - 配置里可能包含敏感信息(数据库密码/API key),**AES-256-GCM 加密存储**(与 R13 一致)
  - **Skills 管理**:
    - **平台级 Skills**:平台官方维护,所有项目可用;超管在"平台设置 → Skills 市场"维护
    - **项目级 Skills**:项目成员在项目设置里**上传自己的 `.md` 文件** 或 **从平台级 Skills 市场选择安装**
    - Skills 内容格式沿用 **Claude Code Skills 原生格式**(YAML frontmatter + Markdown 正文)
  - **任务创建时注入**:
    - 平台把项目级 `mcp_config` 注入到容器 `~/.claude/config.json`(与镜像预装的 MCP server **合并**,项目级覆盖同名)
    - 平台把项目级 Skills 列表对应的 `.md` 文件写入容器 `~/.claude/skills/` 目录(与镜像预装的 Skills **合并**,项目级覆盖同名)
- **不做什么**:
  - 用户级 Skills(跟随用户跨项目;V1 太细)
  - Skills 社区市场(用户分享 Skills;V2 再做)
  - 远程 MCP server(有状态/资源密集/跨任务共享;V2 再做)
  - Skills 版本管理(V1 覆盖式更新)

### 字段定义

(Skill 表)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| skill_id | uuid | 是 | auto | |
| name | string(64) | 是 | - | 全局唯一(kebab-case) |
| description | string(255) | 是 | - | 一句话描述 |
| content | text | 是 | - | Markdown 正文(YAML frontmatter + 正文) |
| scope | enum | 是 | platform | platform(平台级)/ project(项目级) |
| project_id | ref(R2) | 否 | null | scope=project 时必填 |
| created_by | ref(R1) | 是 | - | |
| created_at / updated_at | datetime | 是 | now | |

(ProjectSkill 关联表)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| project_id | ref(R2) | 是 | - | |
| skill_id | ref(Skill) | 是 | - | |
| installed_by | ref(R1) | 是 | - | |
| installed_at | datetime | 是 | now | |

(Project 表新增)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| mcp_config_encrypted | text | 否 | null | 项目级 MCP server 配置(JSON),AES-256-GCM 加密 |

### 交互规则

| 场景/条件 | 行为/规则 | 结果/去向 |
|---|---|---|
| 项目设置 → MCP Tab → 编辑配置 | 用户编辑 JSON → 平台校验 JSON 格式合法 → **AES-256-GCM 加密**存到 `mcp_config_encrypted` | 保存成功 |
| 项目设置 → MCP Tab → 用模板 | 用户选模板(如 PostgreSQL)→ 填参数(host/port/user/password)→ 平台生成 JSON → 加密存储 | 保存成功 |
| 项目设置 → Skills Tab → 从市场安装 | 列出平台级 Skills(scope=platform)→ 用户点击"安装" → 写入 `project_skills` 关联表 | 安装成功 |
| 项目设置 → Skills Tab → 上传自定义 Skill | 用户上传 `.md` 文件 → 平台校验格式(YAML frontmatter 必须有 name/description)→ 写入 `skills` 表(scope=project)+ `project_skills` 关联表 | 上传成功 |
| 项目设置 → Skills Tab → 卸载 | 删除 `project_skills` 关联记录 | 卸载成功 |
| 任务创建时注入 | 平台读 `mcp_config_encrypted` 解密 + 读 `project_skills` 关联的 Skills 内容 → 注入到容器 `~/.claude/config.json` 与 `~/.claude/skills/*.md` | AI 可用 |
| 镜像预装 vs 项目级覆盖 | 同名 MCP server / Skill,**项目级覆盖镜像预装** | |
| 敏感信息 | MCP 配置里的密码/key 在**接口返回时打码**(前 4 位 + 后 4 位,中间 ***);**容器内 env 注入时解密** | 前端不明文 |

### 异常与边界场景

- **MCP 配置 JSON 格式错误**:保存时拒绝,提示具体错误行
- **Skill `.md` 文件格式错误**(缺 YAML frontmatter / name / description):上传时拒绝,提示具体错误
- **Skill name 冲突**(同名 platform Skill 与 project Skill):项目级覆盖平台级
- **MCP server 启动失败**(如数据库连不上):Claude CLI 启动时会报错,任务失败,日志可见
- **Skill 内容包含恶意指令**(如"删除所有文件"):V1 **不拦截**(用户自己负责);V2 可加内容审核

### 验收标准

1. 镜像预装的 MCP server(filesystem/git)在所有任务容器中可用
2. 项目级 MCP 配置(JSON)能保存并在任务中生效
3. 项目级 Skills 能上传/安装/卸载,并在任务中生效
4. 同名 MCP server / Skill 项目级覆盖镜像预装
5. MCP 配置里的敏感信息加密存储,接口返回打码

## 依赖

R2(项目)

## 涉及模块/文件

**后端**:
- `backend/app/models/skill.py`(Skill/ProjectSkill 模型)
- `backend/app/schemas/skill.py`(Pydantic 模型)
- `backend/app/api/projects.py`(MCP 配置接口,追加到 R2 的文件)
- `backend/app/api/skills.py`(Skills 接口)
- `backend/app/api/admin/skills.py`(平台级 Skills 管理,超管)
- `backend/app/services/skill_service.py`(Skills 业务逻辑)
- `backend/app/services/mcp_service.py`(MCP 配置业务逻辑)

**前端**:
- `frontend/src/pages/projects/McpConfigManagement.tsx`(MCP 配置 Tab)
- `frontend/src/pages/projects/SkillsManagement.tsx`(Skills 管理 Tab)
- `frontend/src/pages/admin/SkillsMarket.tsx`(平台级 Skills 市场,超管)
- `frontend/src/api/skills.ts`(API 客户端)

**容器镜像**:
- `docker/devbox/Dockerfile`(预装 MCP server + Skills)
- `docker/devbox/skills/`(平台官方 Skills,`.md` 文件)

## 复用清单

**后端**:
- FastAPI `Depends(get_current_user)` 依赖注入
- SQLAlchemy 2.0 async session
- AES-GCM 加密(`backend/app/core/encryption.py`,R1 已实现)

**前端**:
- shadcn/ui 组件:`Button`, `Input`, `Card`, `Table`, `Dialog`, `Select`, `Badge`, `Alert`, `Textarea`
- react-query `useQuery` / `useMutation`
- lucide-react 图标:`Plus`, `Trash`, `Upload`, `Download`, `Edit`, `Package`, `Book`

## 实现要点

### 数据模型变更

**新增表 `skills`**:

| 字段 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | BIGINT UNSIGNED | 是 | AUTO_INCREMENT | PRIMARY KEY | 内部 id |
| skill_id | CHAR(36) | 是 | UUID | UNIQUE | 对外 id(uuid) |
| name | VARCHAR(64) | 是 | - | UNIQUE | 全局唯一(kebab-case) |
| description | VARCHAR(255) | 是 | - | - | 一句话描述 |
| content | TEXT | 是 | - | - | Markdown 正文(YAML frontmatter + 正文) |
| scope | ENUM('platform', 'project') | 是 | 'platform' | INDEX | 作用域 |
| project_id | CHAR(36) | 否 | NULL | INDEX | scope=project 时必填 |
| created_by | CHAR(36) | 是 | - | - | 创建者 user_id |
| created_at | DATETIME | 是 | CURRENT_TIMESTAMP | - | 创建时间 |
| updated_at | DATETIME | 是 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | - | 更新时间 |

**新增表 `project_skills`**:

| 字段 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | BIGINT UNSIGNED | 是 | AUTO_INCREMENT | PRIMARY KEY | 内部 id |
| project_id | CHAR(36) | 是 | - | INDEX | 项目 id |
| skill_id | CHAR(36) | 是 | - | INDEX | Skill id |
| installed_by | CHAR(36) | 是 | - | - | 安装人 user_id |
| installed_at | DATETIME | 是 | CURRENT_TIMESTAMP | - | 安装时间 |

**约束**:
- `project_skills` 联合唯一索引:`(project_id, skill_id)` 唯一(同一项目不可重复安装同一 Skill)

**修改表 `projects`**:
- 新增字段:`mcp_config_encrypted TEXT`(AES-GCM 加密的 MCP 配置 JSON)

**SQL 全文见 `DEPLOY.md`**

### 接口契约

**接口契约** 总览——**响应结构**统一外层 `{"code": 0, "data": ..., "message": "ok"}`(错误时 `code` 为业务错误码、`message` 为提示文案);字段级结构以下方各接口小节的请求/响应示例为准:

| 接口范围 | 响应结构约定 |
|---|---|
| 本需求点全部接口(下方小节逐一列出) | 统一外层 `{code, data, message}`;分页接口 `data` 含 `{items, total}` |

#### 1. 获取项目级 MCP 配置

- **路径**:`GET /api/projects/{project_id}/mcp-config`
- **鉴权**:JWT(项目成员)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "config": {
        "mcpServers": {
          "postgres": {
            "command": "npx",
            "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://user:***@host:5432/db"]
          }
        }
      }
    },
    "message": "ok"
  }
  ```
  (敏感信息打码)

#### 2. 更新项目级 MCP 配置

- **路径**:`PUT /api/projects/{project_id}/mcp-config`
- **鉴权**:JWT(owner)
- **请求**:
  ```json
  {
    "config": {
      "mcpServers": {
        "postgres": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://user:password@host:5432/db"]
        }
      }
    }
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "message": "保存成功"
  }
  ```
- **错误码**:
  - `17001`: JSON 格式错误(提示具体行)

#### 3. MCP 配置模板列表

- **路径**:`GET /api/projects/{project_id}/mcp-config/templates`
- **鉴权**:JWT(项目成员)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "items": [
        {
          "name": "postgres",
          "display_name": "PostgreSQL",
          "description": "PostgreSQL 数据库",
          "params": [
            {"name": "host", "type": "string", "required": true},
            {"name": "port", "type": "number", "required": true, "default": 5432},
            {"name": "user", "type": "string", "required": true},
            {"name": "password", "type": "string", "required": true, "sensitive": true},
            {"name": "database", "type": "string", "required": true}
          ]
        }
      ]
    },
    "message": "ok"
  }
  ```

#### 4. 平台级 Skills 市场列表

- **路径**:`GET /api/skills`
- **鉴权**:JWT
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "items": [
        {
          "skill_id": "uuid",
          "name": "code-review",
          "description": "代码审查",
          "scope": "platform",
          "created_by": {
            "user_id": "uuid",
            "username": "admin"
          },
          "created_at": "datetime"
        }
      ]
    },
    "message": "ok"
  }
  ```

#### 5. Skill 详情

- **路径**:`GET /api/skills/{skill_id}`
- **鉴权**:JWT
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "skill_id": "uuid",
      "name": "code-review",
      "description": "代码审查",
      "content": "---\nname: code-review\ndescription: Review code changes\n---\n\nWhen reviewing code:\n1. Check for bugs\n2. Check for style\n",
      "scope": "platform"
    },
    "message": "ok"
  }
  ```

#### 6. 项目已安装 Skills 列表

- **路径**:`GET /api/projects/{project_id}/skills`
- **鉴权**:JWT(项目成员)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "items": [
        {
          "skill_id": "uuid",
          "name": "code-review",
          "description": "代码审查",
          "scope": "platform",
          "installed_by": {
            "user_id": "uuid",
            "username": "string"
          },
          "installed_at": "datetime"
        }
      ]
    },
    "message": "ok"
  }
  ```

#### 7. 安装平台级 Skill

- **路径**:`POST /api/projects/{project_id}/skills`
- **鉴权**:JWT(owner/editor)
- **请求**:
  ```json
  {
    "skill_id": "uuid"
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "message": "安装成功"
  }
  ```
- **错误码**:
  - `17002`: 已安装过该 Skill

#### 8. 上传项目级自定义 Skill

- **路径**:`POST /api/projects/{project_id}/skills/upload`
- **鉴权**:JWT(owner/editor)
- **请求**:multipart/form-data
  - `file`: `.md` 文件
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "skill_id": "uuid",
      "name": "custom-skill",
      "description": "自定义 Skill"
    },
    "message": "上传成功"
  }
  ```
- **错误码**:
  - `17003`: 文件格式错误(缺 YAML frontmatter / name / description)

#### 9. 卸载 Skill

- **路径**:`DELETE /api/projects/{project_id}/skills/{skill_id}`
- **鉴权**:JWT(owner/editor)
- **响应**:
  ```json
  {
    "code": 0,
    "message": "卸载成功"
  }
  ```

#### 10. 平台级 Skills 管理(超管)

- **路径**:`GET/POST /api/admin/skills`,`PATCH/DELETE /api/admin/skills/{id}`
- **鉴权**:JWT(超管)
- (接口结构同上,不再赘述)

### 权限矩阵

| 操作 | owner | editor | viewer |
|---|---|---|---|
| 查看 MCP 配置 | ✅ | ✅ | ✅(敏感信息打码) |
| 更新 MCP 配置 | ✅ | ❌ | ❌ |
| 查看 Skills 市场 | ✅ | ✅ | ✅ |
| 查看项目已安装 Skills | ✅ | ✅ | ✅ |
| 安装/上传/卸载 Skills | ✅ | ✅ | ❌ |
| 平台级 Skills 管理 | ❌(超管) | ❌ | ❌ |

### 服务端行为规格

#### MCP 配置注入(任务创建时)

1. 平台读 `projects.mcp_config_encrypted` → AES-GCM 解密
2. 与镜像预装的 `~/.claude/config.json` **合并**(项目级覆盖同名)
3. 写入容器 `~/.claude/config.json`

#### Skills 注入(任务创建时)

1. 平台读 `project_skills` 关联的 Skills 内容(从 `skills` 表)
2. 写入容器 `~/.claude/skills/{name}.md`(项目级覆盖镜像预装同名)

#### Skill 文件格式校验

- 必须有 YAML frontmatter:
  ```markdown
  ---
  name: skill-name
  description: 一句话描述
  ---

  Markdown 正文
  ```
- 缺 `name` / `description` → 返回 `17003`

#### MCP 配置敏感信息打码

- 遍历 `mcpServers` 的每个 server 的 `args` 与 `env`
- 若值包含 `password` / `token` / `key` 等关键词 → 打码(前 4 + 后 4)

### 页面结构说明

#### MCP 配置 Tab `/projects/{project_id}/settings/mcp`

- **区块结构**:
  1. **操作栏**:右侧"使用模板"按钮(secondary)
  2. **JSON 编辑器**:
     - 全宽 Textarea(等宽字体,20 行)
     - 底部提示:"配置将注入到任务容器的 `~/.claude/config.json`"
  3. **保存按钮**(primary,右下角)
  4. **使用模板对话框**:
     - 模板列表(卡片):PostgreSQL / Redis / GitHub / Slack 等
     - 点击模板 → 显示参数表单(根据 `params` 动态生成)
     - 填参数 → 点"生成" → 平台生成 JSON → 填入编辑器

#### Skills 管理 Tab `/projects/{project_id}/settings/skills`

- **区块结构**:
  1. **操作栏**:右侧"从市场安装"按钮(secondary)+ "上传自定义"按钮(primary)
  2. **已安装 Skills 列表**(Table):
     - 列:Skill 名 / 描述 / 来源徽章(platform=secondary/project=primary)/ 安装人 / 安装时间 / 操作(查看/卸载)
  3. **从市场安装对话框**:
     - 平台级 Skills 列表(卡片):名称 + 描述 + "安装"按钮
  4. **上传自定义 Skill 对话框**:
     - 文件上传 Input(accept=".md")
     - "取消" + "上传"按钮
  5. **Skill 详情对话框**:
     - 显示 Skill 内容(Markdown 渲染)

### 页面交互流程

#### 更新 MCP 配置流程

1. 用户进入 MCP 配置 Tab → 显示当前配置(JSON)
2. 用户编辑 JSON → 点"保存"
3. 前端校验 JSON 格式 → 调 `PUT /api/projects/{pid}/mcp-config`
4. 成功 → 提示"保存成功"
5. 失败 → 显示错误提示(JSON 格式错误,提示具体行)

#### 使用模板流程

1. 用户点"使用模板" → 打开对话框
2. 选择模板(如 PostgreSQL)→ 显示参数表单
3. 填参数(host/port/user/password/database)→ 点"生成"
4. 平台生成 JSON → 填入编辑器
5. 用户确认 → 点"保存"

#### 安装 Skill 流程

1. 用户点"从市场安装" → 打开对话框
2. 浏览平台级 Skills 列表 → 点"安装"
3. 调 `POST /api/projects/{pid}/skills`
4. 成功 → 关闭对话框 → 刷新已安装列表

#### 上传自定义 Skill 流程

1. 用户点"上传自定义" → 打开对话框
2. 选择 `.md` 文件 → 点"上传"
3. 调 `POST /api/projects/{pid}/skills/upload`
4. 成功 → 关闭对话框 → 刷新已安装列表

### 控件联动

| 触发控件 | 事件 | 响应行为 |
|---|---|---|
| JSON 编辑器 | 输入 | 实时校验 JSON 格式,错误行高亮 |
| 模板卡片 | 点击 | 显示参数表单 |
| Skill 文件上传 Input | 选择文件 | 显示文件名 + 大小 |

### 文案清单

| 场景 | 文案 |
|---|---|
| MCP 配置 Tab 标题 | MCP 配置 |
| 使用模板按钮 | 使用模板 |
| 保存按钮 | 保存 |
| JSON 编辑器提示 | 配置将注入到任务容器的 `~/.claude/config.json` |
| 使用模板对话框标题 | 使用模板 |
| 生成按钮 | 生成 |
| Skills 管理 Tab 标题 | Skills 管理 |
| 从市场安装按钮 | 从市场安装 |
| 上传自定义按钮 | 上传自定义 |
| 已安装 Skills 列表列-Skill 名 | Skill 名 |
| 已安装 Skills 列表列-描述 | 描述 |
| 已安装 Skills 列表列-来源 | 来源 |
| 已安装 Skills 列表列-安装人 | 安装人 |
| 已安装 Skills 列表列-安装时间 | 安装时间 |
| 已安装 Skills 列表列-操作 | 操作 |
| 来源徽章-platform | 平台 |
| 来源徽章-project | 项目 |
| 查看按钮 | 查看 |
| 卸载按钮 | 卸载 |
| 从市场安装对话框标题 | 从市场安装 |
| 安装按钮 | 安装 |
| 上传自定义 Skill 对话框标题 | 上传自定义 Skill |
| 上传按钮 | 上传 |
| Skill 详情对话框标题 | Skill 详情 |
| 错误提示-JSON 格式错误 | JSON 格式错误:第 {line} 行 |
| 错误提示-已安装过 | 已安装过该 Skill |
| 错误提示-文件格式错误 | 文件格式错误:缺少 YAML frontmatter 的 name 或 description |
| 成功提示-保存成功 | 保存成功 |
| 成功提示-安装成功 | 安装成功 |
| 成功提示-上传成功 | 上传成功 |
| 成功提示-卸载成功 | 卸载成功 |

### 设计规范

**Token**:同 R1

**布局**:
- MCP 配置 Tab:操作栏(右侧按钮)+ JSON 编辑器(全宽 Textarea)+ 保存按钮(右下角)
- Skills 管理 Tab:操作栏(右侧按钮)+ Table

**组件**:
- Table:同 R2
- Dialog:同 R2
- Textarea:shadcn/ui `Textarea` 组件(等宽字体,20 行)
- Badge:platform=secondary/project=primary

**状态**:
- 按钮/输入框/Table 行:同 R2

**效果**:
- 过渡:同 R2
- Dialog:同 R2

### 渲染核对期望

**结构**:
- JSON 编辑器:全宽,等宽字体(Monaco/Geist Mono),20 行
- Skills 列表:Table 列宽自适应,操作列固定宽度 150px

**Token**:同 R2

**状态**:同 R2

**效果**:同 R2

### 设计还原风险清单

(无设计稿,按 DESIGN.md + shadcn/ui New York 规范实现)

## 完成判据

### 功能

1. ✅ owner 能编辑项目级 MCP 配置(JSON)
2. ✅ owner 能使用模板生成 MCP 配置(PostgreSQL/Redis/GitHub 等)
3. ✅ owner/editor 能从市场安装平台级 Skills
4. ✅ owner/editor 能上传项目级自定义 Skills(.md 文件)
5. ✅ owner/editor 能卸载 Skills
6. ✅ 超管能管理平台级 Skills(CRUD)
7. ✅ 任务创建时,MCP 配置注入到容器 `~/.claude/config.json`(项目级覆盖镜像预装)
8. ✅ 任务创建时,Skills 注入到容器 `~/.claude/skills/*.md`(项目级覆盖镜像预装)
9. ✅ MCP 配置里的敏感信息加密存储,接口返回打码

### 异常场景

1. ✅ JSON 格式错误:返回 `17001`(提示具体行)
2. ✅ 已安装过该 Skill:返回 `17002`
3. ✅ Skill 文件格式错误:返回 `17003`(缺 YAML frontmatter / name / description)

### UI

1. ✅ MCP 配置 Tab:JSON 编辑器(等宽字体,20 行)+ 保存按钮 + 使用模板按钮
2. ✅ Skills 管理 Tab:Table 展示 Skill 名/描述/来源/安装人/安装时间/操作
3. ✅ 从市场安装对话框:卡片列表展示平台级 Skills
4. ✅ 上传自定义 Skill 对话框:文件上传 Input(accept=".md")
5. ✅ 所有文案符合"文案清单"

## 测试验证逻辑

### 单元测试(后端)

- `test_update_mcp_config_success`:更新 MCP 配置成功
- `test_update_mcp_config_invalid_json`:JSON 格式错误返回 17001
- `test_install_skill_success`:安装 Skill 成功
- `test_install_skill_already_installed`:已安装过返回 17002
- `test_upload_skill_success`:上传 Skill 成功
- `test_upload_skill_invalid_format`:文件格式错误返回 17003
- `test_uninstall_skill_success`:卸载 Skill 成功
- `test_mcp_config_masked`:接口返回敏感信息打码

### 集成测试(前端)

- 更新 MCP 配置流程:编辑 JSON → 点保存 → 提示成功
- 使用模板流程:点使用模板 → 选模板 → 填参数 → 点生成 → 点保存
- 安装 Skill 流程:点从市场安装 → 点安装 → 刷新列表
- 上传 Skill 流程:点上传自定义 → 选文件 → 点上传 → 刷新列表

### 手动测试

- 创建任务后,进入容器检查 `~/.claude/config.json` 是否包含项目级 MCP 配置
- 检查 `~/.claude/skills/` 目录是否包含项目级 Skills 的 `.md` 文件
- 用 Claude CLI 验证 MCP server 与 Skills 生效

## 实现进度

(rd-dev 维护)

- [ ] 数据模型(skills / project_skills 表 + projects 表加 mcp_config_encrypted 字段)
- [ ] 接口(MCP 配置读写/模板列表/Skills 市场/项目已安装 Skills/安装/上传/卸载/平台级 Skills 管理)
- [ ] 前端页面(MCP 配置 Tab/Skills 管理 Tab/从市场安装对话框/上传自定义对话框/Skill 详情对话框)
- [ ] 容器镜像(预装 MCP server + Skills)
- [ ] 单元测试
- [ ] 集成测试
- [ ] 手动测试
