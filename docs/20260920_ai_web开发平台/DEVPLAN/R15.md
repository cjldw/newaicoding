# R15:平台网关与域名(支持 Runner 代理)

> 状态:⬜ 未开始
> 模块:M6 网关与路由
> 依赖:R8、R16

## PRD 引用

### 边界定义

- **做什么**:
  - 泛域名 `*.preview.coding-console.zhanqitv.com.cn`(预览,任务级)
  - 主域 `{slug}.coding-console.zhanqitv.com.cn:{port}`(部署,项目级 + 用户自定义端口)
  - 动态路由注册/摘除(容器端口监听驱动 / 部署事件驱动)
  - **路由 upstream 指向 Runner 的本地代理**:`http://{runner_host}:{runner_mapped_port}`(Runner 上的本地代理再转发到容器)
  - 预览路由**鉴权**,部署路由**公开**
  - WebSocket 透传(HMR/TTY)
  - 流量/访问日志
- **不做什么(V1)**:
  - HTTPS(HTTP only)
  - CDN
  - WAF/防火墙(V1 信任内网/基础防护即可)
  - 自定义域名

### 字段定义

(Route 表)
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| route_id | uuid | 是 | |
| host | string | 是 | 完整 host(含端口) |
| upstream | string | 是 | **`http://{runner_host}:{runner_mapped_port}`**(Runner 上的本地代理地址) |
| type | enum | 是 | preview / deploy |
| project_id | ref(R2) | 是 | |
| task_id | ref(R4) | 否 | preview 必填 |
| container_id | ref(R8) | 是 | |
| runner_id | ref(R16) | 是 | |
| auth_required | bool | 是 | preview=true, deploy=false |
| status | enum | 是 | active/inactive |

### 交互规则

| 场景/条件 | 行为/规则 | 结果/去向 |
|---|---|---|
| 注册路由 | 端口监听/部署事件触发 → 平台查询容器的 `runner_id` + `runner_host_port_*` → 拼接 upstream → 注册到网关 | active |
| 摘除 | 端口关闭/容器销毁/下线 | inactive |
| 访问 preview | 校验 JWT + 项目成员 → 转发到 Runner 上的本地代理 → 再到容器 | 非成员 403 |
| 访问 deploy | 公开 → 转发到 deploy Runner 上的本地代理 → 再到容器 | |
| 端口冲突 | deploy_port 全平台唯一,冲突拒绝 | |
| **Runner offline** | 路由保留,但 upstream 不可达 → 网关返回 502 + "Runner offline" 提示 | |

### 异常与边界场景

- 上游 5xx → 502 页面带项目名与日志链接
- 单 host QPS 上限(防爬/防滥用,默认 100)
- 端口池 `10000-10099`(100 个端口,够 20 个项目 × 5 个部署)
- **Runner 本地代理端口冲突**:Runner 启动容器时分配随机宿主机端口(范围 20000-29999),冲突自动重试

### 验收标准

1. 预览鉴权正确,部署公开
2. WebSocket 透传正常(HMR/TTY)
3. 端口冲突被拒绝
4. 容器销毁后路由自动摘除
5. Runner offline 时路由保留但返回 502 + 明确提示
6. 路由 upstream 正确指向 Runner 的本地代理

## 依赖

- R8(容器)
- R16(Runner)

## 涉及模块/文件

**后端**:
- `backend/app/models/route.py`(Route 模型)
- `backend/app/services/route_service.py`(路由管理)
- `backend/app/core/gateway.py`(网关核心,反向代理)

**网关**(独立进程):
- `gateway/main.py`(Nginx/Traefik 配置管理,或自研 Python 网关)

## 复用清单

**后端**:
- FastAPI `Depends(get_current_user)` 依赖注入
- SQLAlchemy 2.0 async session

**网关**:
- Nginx(反向代理)或 Traefik(动态路由)

## 实现要点

### 数据模型变更

**新增表 `routes`**:

| 字段 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | BIGINT UNSIGNED | 是 | AUTO_INCREMENT | PRIMARY KEY | 内部 id |
| route_id | CHAR(36) | 是 | UUID | UNIQUE | 对外 id(uuid) |
| host | VARCHAR(255) | 是 | - | UNIQUE | 完整 host(含端口) |
| upstream | VARCHAR(255) | 是 | - | - | Runner 上的本地代理地址 |
| type | ENUM('preview', 'deploy') | 是 | - | INDEX | 类型 |
| project_id | CHAR(36) | 是 | - | INDEX | 项目 id |
| task_id | CHAR(36) | 否 | NULL | INDEX | 任务 id(preview 必填) |
| container_id | VARCHAR(64) | 是 | - | INDEX | 容器 id |
| runner_id | CHAR(36) | 是 | - | INDEX | Runner id |
| auth_required | TINYINT(1) | 是 | 1 | - | 是否鉴权 |
| status | ENUM('active', 'inactive') | 是 | 'inactive' | INDEX | 状态 |
| created_at | DATETIME | 是 | CURRENT_TIMESTAMP | - | 创建时间 |
| updated_at | DATETIME | 是 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | - | 更新时间 |

**SQL 全文见 `DEPLOY.md`**

### 接口契约

(网关是基础设施,无对外 API;平台后端通过内部接口管理路由)

### 权限矩阵

(网关鉴权在网关层实现,不依赖后端接口)

### 服务端行为规格

#### 注册路由

1. 平台监听容器端口监听事件(Runner 回报 `port_listening`)
2. 平台查询容器的 `runner_id` + `runner_host_port_*`
3. 拼接 upstream:`http://{runner_host}:{runner_mapped_port}`
4. 平台写 `routes` 表(status=active)
5. 平台通知网关(Nginx/Traefik)动态加载路由配置

#### 摘除路由

1. 平台监听容器端口关闭事件(Runner 回报 `port_closed`)或容器销毁事件
2. 平台更新 `routes` 表(status=inactive)
3. 平台通知网关卸载路由配置

#### 访问 preview

1. 用户访问 `http://{slug}--{taskId}--{port}.preview.coding-console.zhanqitv.com.cn`
2. 网关查 `routes` 表(host 匹配 + type=preview + status=active)
3. 校验 JWT + 项目成员(R12)
4. 鉴权通过 → 转发到 upstream(Runner 上的本地代理)→ 再到容器
5. 鉴权失败 → 返回 403

#### 访问 deploy

1. 用户访问 `http://{slug}.coding-console.zhanqitv.com.cn:{port}`
2. 网关查 `routes` 表(host 匹配 + type=deploy + status=active)
3. **不鉴权**(公开访问)
4. 转发到 upstream(deploy Runner 上的本地代理)→ 再到容器

### 页面结构说明

(网关是基础设施,无前端页面)

### 页面交互流程

(无)

### 控件联动

(无)

### 文案清单

(无)

### 设计规范

(无)

### 渲染核对期望

(无)

### 设计还原风险清单

(无)

## 完成判据

### 功能

1. ✅ 预览鉴权正确(非成员 403)
2. ✅ 部署公开访问
3. ✅ WebSocket 透传正常(HMR/TTY)
4. ✅ 端口冲突被拒绝(deploy_port 全平台唯一)
5. ✅ 容器销毁后路由自动摘除
6. ✅ Runner offline 时路由保留但返回 502 + 明确提示
7. ✅ 路由 upstream 正确指向 Runner 的本地代理

### 异常场景

1. ✅ 上游 5xx → 502 页面带项目名与日志链接
2. ✅ 单 host QPS 上限(默认 100)

### UI

(无)

## 测试验证逻辑

### 单元测试(后端)

- `test_register_route_success`:注册路由成功
- `test_unregister_route_success`:摘除路由成功
- `test_preview_auth_required`:预览鉴权(非成员 403)
- `test_deploy_public_access`:部署公开访问

### 集成测试

- 容器起 5173 服务 → 预览 URL 可访问
- 容器销毁 → 预览 URL 不可访问

### 手动测试

- 在容器里跑 `npm run dev`,验证预览 URL 可访问
- 用非成员账号访问预览 URL,验证返回 403
- 部署任务 deployed 后,验证部署 URL 公开可访问

## 实现进度

(rd-dev 维护)

- [ ] 数据模型(routes 表)
- [ ] 路由管理(注册/摘除)
- [ ] 网关核心(Nginx/Traefik 配置管理)
- [ ] 预览鉴权(JWT + 项目成员)
- [ ] WebSocket 透传
- [ ] 单元测试
- [ ] 集成测试
- [ ] 手动测试
