# R18:站内信与通知

> 状态:⬜ 未开始
> 模块:M8 通知(新增)
> 依赖:R1、R2、R4

## PRD 引用

### 边界定义

- **做什么**:
  - **站内信中心**:列表 + 详情 + 已读/未读/删除 + 分类 Tab(全部/告警/任务/项目)
  - **钉钉 webhook**:个人级(用户绑定自己的 webhook)+ 项目级(owner 绑定项目钉钉群 webhook)
  - **实时推送**:通知到达时,前端**实时弹 toast**(走 D5 统一 WebSocket 网关,频道 `/ws/notifications`)
  - **通知分级**:critical(紧急)/ normal(普通)/ info(信息),不同级别走不同通道
- **不做什么**:
  - 邮件通知(仅站内信 + 钉钉)
  - 通知模板自定义(V1 内置模板)
  - 通知订阅规则自定义(V1 内置接收人规则)

### 字段定义

(Notification 表)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| notification_id | uuid | 是 | auto | |
| recipient_id | ref(R1) | 是 | - | 接收人 |
| type | enum | 是 | - | deploy_failed / runner_offline / task_failed / push_failed / review_approved / review_rejected / invited_to_project / task_done / deployed |
| level | enum | 是 | normal | critical / normal / info |
| title | string(255) | 是 | - | |
| content | text | 是 | - | Markdown |
| link | string(255) | 否 | null | 跳转链接 |
| project_id | ref(R2) | 否 | null | 关联项目(可空) |
| task_id | ref(R4) | 否 | null | 关联任务(可空) |
| read_at | datetime | 否 | null | 已读时间(null=未读) |
| created_at | datetime | 是 | now | |

(UserNotificationSettings 表)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| user_id | ref(R1) | 是 | - | 唯一 |
| dingtalk_webhook | string(255) | 否 | null | 个人钉钉 webhook URL |
| dingtalk_enabled | bool | 是 | false | 是否启用钉钉通知 |
| realtime_toast_enabled | bool | 是 | true | 是否启用实时 toast |

(Project 表新增)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| dingtalk_webhook | string(255) | 否 | null | 项目钉钉群 webhook URL |
| dingtalk_enabled | bool | 是 | false | 是否启用项目钉钉通知 |

### 交互规则

(通知级别 × 通道矩阵)
| 级别 | 站内信 | 钉钉(个人) | 钉钉(项目) | 实时 toast |
|---|---|---|---|---|
| critical | ✅ | ✅(若启用) | ✅(若启用) | ✅(若启用) |
| normal | ✅ | ❌ | ❌ | ✅(若启用) |
| info | ✅ | ❌ | ❌ | ❌ |

(通知场景与接收人)
| 场景 | type | level | 接收人 | 通道 |
|---|---|---|---|---|
| 部署容器崩溃 | deploy_failed | critical | 项目 owner + 部署操作人 | 站内信 + 钉钉(个人+项目) + toast |
| Runner 全部 offline | runner_offline | critical | 超管 | 站内信 + 钉钉(个人) + toast |
| 任务 failed | task_failed | normal | 任务创建者 | 站内信 + toast |
| 容器销毁前 push 失败 | push_failed | critical | 任务创建者 | 站内信 + 钉钉(个人) + toast |
| 需求评审通过 | review_approved | normal | 需求创建者 | 站内信 + toast |
| 需求评审驳回 | review_rejected | normal | 需求创建者 | 站内信 + toast |
| 被邀请加入项目 | invited_to_project | normal | 被邀请人 | 站内信 + toast |
| 任务完成 | task_done | info | 任务创建者 | 站内信 |
| 部署成功 | deployed | info | 项目 owner + 部署操作人 | 站内信 |

### 异常与边界场景

- 钉钉 webhook 失效(调用返回 4xx):记录失败日志,不影响站内信;连续失败 3 次标记 webhook 失效,通知用户重新配置
- 实时 toast 关闭时:仅写站内信,不弹 toast
- 用户离线时:站内信正常入库,用户下次上线时看到未读计数

### 验收标准

1. 部署崩溃时,项目 owner + 部署操作人收到站内信 + 钉钉 + toast
2. 任务失败时,任务创建者收到站内信 + toast
3. 用户能在"通知中心"查看历史通知,标记已读/删除
4. 用户能在"个人设置 → 通知设置"绑定钉钉 webhook
5. 项目 owner 能在项目设置绑定项目钉钉群 webhook
6. 钉钉 webhook 失效时,连续失败 3 次后通知用户重新配置

## 依赖

- R1(用户)
- R2(项目)
- R4(任务)

## 涉及模块/文件

**后端**:
- `backend/app/models/notification.py`(Notification/UserNotificationSettings 模型)
- `backend/app/schemas/notification.py`(Pydantic 模型)
- `backend/app/api/notifications.py`(通知接口)
- `backend/app/services/notification_service.py`(通知业务逻辑)
- `backend/app/services/dingtalk_service.py`(钉钉 webhook 调用)
- `backend/app/core/websocket.py`(WebSocket 网关,通知频道 `/ws/notifications`)

**前端**:
- `frontend/src/pages/notifications/NotificationCenter.tsx`(通知中心)
- `frontend/src/pages/settings/NotificationSettings.tsx`(通知设置)
- `frontend/src/components/NotificationBell.tsx`(导航栏通知铃铛,未读计数)
- `frontend/src/components/NotificationToast.tsx`(实时 toast)
- `frontend/src/api/notifications.ts`(API 客户端)

## 复用清单

**后端**:
- FastAPI `Depends(get_current_user)` 依赖注入
- SQLAlchemy 2.0 async session
- `httpx`(async)调钉钉 webhook
- WebSocket 网关(D5)

**前端**:
- shadcn/ui 组件:`Button`, `Card`, `Table`, `Tabs`, `Badge`, `Dialog`, `Input`, `Switch`, `Toast`
- react-query `useQuery` / `useMutation`
- Zustand `notificationStore`(未读计数)
- lucide-react 图标:`Bell`, `BellDot`, `Check`, `Trash`, `X`, `Settings`

## 实现要点

### 数据模型变更

**新增表 `notifications`**:

| 字段 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | BIGINT UNSIGNED | 是 | AUTO_INCREMENT | PRIMARY KEY | 内部 id |
| notification_id | CHAR(36) | 是 | UUID | UNIQUE | 对外 id(uuid) |
| recipient_id | CHAR(36) | 是 | - | INDEX | 接收人 user_id |
| type | ENUM('deploy_failed', 'runner_offline', 'task_failed', 'push_failed', 'review_approved', 'review_rejected', 'invited_to_project', 'task_done', 'deployed') | 是 | - | INDEX | 类型 |
| level | ENUM('critical', 'normal', 'info') | 是 | 'normal' | INDEX | 级别 |
| title | VARCHAR(255) | 是 | - | - | 标题 |
| content | TEXT | 是 | - | - | 内容(Markdown) |
| link | VARCHAR(255) | 否 | NULL | - | 跳转链接 |
| project_id | CHAR(36) | 否 | NULL | INDEX | 关联项目 id |
| task_id | CHAR(36) | 否 | NULL | INDEX | 关联任务 id |
| read_at | DATETIME | 否 | NULL | INDEX | 已读时间 |
| created_at | DATETIME | 是 | CURRENT_TIMESTAMP | INDEX | 创建时间 |

**新增表 `user_notification_settings`**:

| 字段 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | BIGINT UNSIGNED | 是 | AUTO_INCREMENT | PRIMARY KEY | 内部 id |
| user_id | CHAR(36) | 是 | - | UNIQUE | 用户 id |
| dingtalk_webhook | VARCHAR(255) | 否 | NULL | - | 钉钉 webhook URL |
| dingtalk_enabled | TINYINT(1) | 是 | 0 | - | 是否启用钉钉 |
| realtime_toast_enabled | TINYINT(1) | 是 | 1 | - | 是否启用实时 toast |
| dingtalk_fail_count | INT | 是 | 0 | - | 钉钉 webhook 连续失败次数 |
| created_at | DATETIME | 是 | CURRENT_TIMESTAMP | - | 创建时间 |
| updated_at | DATETIME | 是 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | - | 更新时间 |

**修改表 `projects`**:
- 新增字段:`dingtalk_webhook VARCHAR(255)`(项目钉钉群 webhook URL)
- 新增字段:`dingtalk_enabled TINYINT(1) DEFAULT 0`(是否启用项目钉钉通知)

**SQL 全文见 `DEPLOY.md`**

### 接口契约

#### 1. 通知列表

- **路径**:`GET /api/notifications`
- **鉴权**:JWT
- **请求**:
  - Query 参数:`type`(可选),`level`(可选),`read`(可选,true/false),`page`,`page_size`
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "items": [
        {
          "notification_id": "uuid",
          "type": "deploy_failed",
          "level": "critical",
          "title": "部署失败",
          "content": "项目 xxx 的部署容器崩溃了",
          "link": "/tasks/xxx/deploy",
          "project": {
            "project_id": "uuid",
            "name": "string"
          },
          "read_at": null,
          "created_at": "datetime"
        }
      ],
      "total": 50,
      "unread_count": 10,
      "page": 1,
      "page_size": 20
    },
    "message": "ok"
  }
  ```

#### 2. 标记已读

- **路径**:`POST /api/notifications/{notification_id}/read`
- **鉴权**:JWT
- **响应**:
  ```json
  {
    "code": 0,
    "message": "已标记为已读"
  }
  ```

#### 3. 全部标记已读

- **路径**:`POST /api/notifications/read-all`
- **鉴权**:JWT
- **响应**:
  ```json
  {
    "code": 0,
    "message": "全部标记为已读"
  }
  ```

#### 4. 删除通知

- **路径**:`DELETE /api/notifications/{notification_id}`
- **鉴权**:JWT
- **响应**:
  ```json
  {
    "code": 0,
    "message": "已删除"
  }
  ```

#### 5. 获取未读计数

- **路径**:`GET /api/notifications/unread-count`
- **鉴权**:JWT
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "unread_count": 10
    },
    "message": "ok"
  }
  ```

#### 6. 获取个人通知设置

- **路径**:`GET /api/users/me/notification-settings`
- **鉴权**:JWT
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "dingtalk_webhook": "https://oapi.dingtalk.com/robot/send?access_token=xxx",
      "dingtalk_enabled": true,
      "realtime_toast_enabled": true
    },
    "message": "ok"
  }
  ```

#### 7. 更新个人通知设置

- **路径**:`PUT /api/users/me/notification-settings`
- **鉴权**:JWT
- **请求**:
  ```json
  {
    "dingtalk_webhook": "string(可选)",
    "dingtalk_enabled": "bool(可选)",
    "realtime_toast_enabled": "bool(可选)"
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "message": "保存成功"
  }
  ```

#### 8. 测试钉钉 webhook

- **路径**:`POST /api/users/me/notification-settings/test-dingtalk`
- **鉴权**:JWT
- **响应**:
  ```json
  {
    "code": 0,
    "message": "测试消息已发送,请检查钉钉群"
  }
  ```
- **错误码**:
  - `18001`: webhook 无效(调用返回 4xx)

#### 9. 项目钉钉设置(owner)

- **路径**:`PUT /api/projects/{project_id}/dingtalk-settings`
- **鉴权**:JWT(owner)
- **请求**:
  ```json
  {
    "dingtalk_webhook": "string",
    "dingtalk_enabled": true
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "message": "保存成功"
  }
  ```

#### WebSocket 通知频道

- **路径**:`/ws/notifications`
- **鉴权**:JWT(WebSocket 握手时校验)
- **消息协议**:
  - 平台 → 前端:
    ```json
    {"type": "notification", "notification_id": "uuid", "level": "critical", "title": "部署失败", "content": "...", "link": "/tasks/xxx/deploy"}
    ```

### 权限矩阵

| 操作 | 已登录用户 |
|---|---|
| 查看自己的通知列表 | ✅ |
| 标记已读/删除自己的通知 | ✅ |
| 查看/更新自己的通知设置 | ✅ |
| 配置项目钉钉 webhook | owner only |

### 服务端行为规格

#### 发送通知

1. 业务事件触发(如任务 failed)
2. 平台创建通知:
   ```python
   notification = Notification(
       recipient_id=user_id,
       type="task_failed",
       level="normal",
       title="任务失败",
       content=f"任务 {task.title} 执行失败:{task.error_message}",
       link=f"/tasks/{task.task_id}",
       project_id=task.project_id,
       task_id=task.task_id
   )
   await db.add(notification)
   await db.commit()
   ```
3. 平台通过 WebSocket 推送实时 toast(若用户启用):
   ```python
   await websocket_manager.send_to_user(user_id, {
       "type": "notification",
       "notification_id": notification.notification_id,
       "level": notification.level,
       "title": notification.title,
       "content": notification.content,
       "link": notification.link
   })
   ```
4. 若 level=critical 且用户启用钉钉 → 调钉钉 webhook:
   ```python
   if notification.level == "critical" and user_settings.dingtalk_enabled:
       await dingtalk_service.send(user_settings.dingtalk_webhook, {
           "msgtype": "markdown",
           "markdown": {
               "title": notification.title,
               "text": f"### {notification.title}\n\n{notification.content}\n\n[查看详情]({notification.link})"
           }
       })
   ```
5. 若 level=critical 且项目启用钉钉 → 调项目钉钉 webhook(同上)

#### 钉钉 webhook 失败处理

1. 调钉钉 webhook 失败(4xx)→ 记录失败日志,`dingtalk_fail_count + 1`
2. 连续失败 3 次 → 标记 webhook 失效(`dingtalk_enabled = false`)→ 发送站内信通知用户重新配置

### 页面结构说明

#### 通知中心 `/notifications`

- **区块结构**:
  1. **页面标题**:"通知中心"
  2. **操作栏**:
     - 左侧:分类 Tab(全部/告警/任务/项目)+ 未读过滤 Switch
     - 右侧:"全部已读"按钮(secondary)
  3. **通知列表**(Table):
     - 列:级别徽章(critical=danger/normal=primary/info=outline)/ 标题 / 内容(前 100 字)/ 项目(若有)/ 时间 / 状态(未读加粗)/ 操作(查看/删除)
  4. **分页器**:底部居中

#### 通知设置页 `/settings/notifications`

- **区块结构**:
  1. **页面标题**:"通知设置"
  2. **钉钉通知卡片**:
     - 钉钉 webhook 输入框(label="钉钉 Webhook URL",placeholder="https://oapi.dingtalk.com/robot/send?access_token=xxx")
     - 启用钉钉通知 Switch(label="启用钉钉通知")
     - "测试"按钮(secondary)
     - "保存"按钮(primary)
  3. **实时通知卡片**:
     - 启用实时 toast Switch(label="启用实时通知(页面右下角弹窗)")

#### 导航栏通知铃铛

- **区块结构**:
  1. **铃铛图标**:有未读时显示红点 + 未读计数
  2. **点击下拉**:显示最近 5 条未读通知 + "查看全部"链接

### 页面交互流程

#### 查看通知流程

1. 用户点导航栏铃铛 → 下拉显示最近 5 条未读
2. 点"查看全部" → 跳转通知中心
3. 点某条通知 → 标记已读 + 跳转 link(若有)

#### 实时 toast 流程

1. 平台推送通知到 WebSocket 频道 `/ws/notifications`
2. 前端收到消息 → 弹 toast(右下角,5s 自动消失)
3. 用户点 toast → 标记已读 + 跳转 link

### 控件联动

| 触发控件 | 事件 | 响应行为 |
|---|---|---|
| 未读过滤 Switch | 切换 | 重新加载列表(仅显示未读) |
| 通知行 | 点击 | 标记已读 + 跳转 link |
| 测试钉钉按钮 | 点击 | 调测试接口 → 显示成功/失败 |

### 文案清单

| 场景 | 文案 |
|---|---|
| 通知中心页标题 | 通知中心 |
| 分类 Tab-全部 | 全部 |
| 分类 Tab-告警 | 告警 |
| 分类 Tab-任务 | 任务 |
| 分类 Tab-项目 | 项目 |
| 未读过滤 Switch label | 仅显示未读 |
| 全部已读按钮 | 全部已读 |
| 通知列表列-级别 | 级别 |
| 通知列表列-标题 | 标题 |
| 通知列表列-内容 | 内容 |
| 通知列表列-项目 | 项目 |
| 通知列表列-时间 | 时间 |
| 通知列表列-操作 | 操作 |
| 级别徽章-critical | 紧急 |
| 级别徽章-normal | 普通 |
| 级别徽章-info | 信息 |
| 查看按钮 | 查看 |
| 删除按钮 | 删除 |
| 通知设置页标题 | 通知设置 |
| 钉钉通知卡片标题 | 钉钉通知 |
| 钉钉 webhook label | 钉钉 Webhook URL |
| 启用钉钉通知 Switch label | 启用钉钉通知 |
| 测试按钮 | 测试 |
| 保存按钮 | 保存 |
| 实时通知卡片标题 | 实时通知 |
| 启用实时 toast Switch label | 启用实时通知(页面右下角弹窗) |
| 成功提示-已标记为已读 | 已标记为已读 |
| 成功提示-全部标记为已读 | 全部标记为已读 |
| 成功提示-已删除 | 已删除 |
| 成功提示-保存成功 | 保存成功 |
| 成功提示-测试消息已发送 | 测试消息已发送,请检查钉钉群 |
| 错误提示-webhook 无效 | 钉钉 Webhook 无效,请检查后重试 |

### 设计规范

**Token**:同 R1

**布局**:
- 通知中心:页面标题 + 操作栏(Tab + Switch + 按钮)+ Table + 分页器
- 通知设置:页面标题 + 卡片(钉钉通知 + 实时通知)

**组件**:
- Table:同 R2
- Tabs:shadcn/ui `Tabs` 组件
- Switch:shadcn/ui `Switch` 组件
- Badge:critical=danger/normal=primary/info=outline
- Toast:shadcn/ui `Toast` 组件(右下角,5s 自动消失)

**状态**:
- 按钮/输入框/Table 行:同 R2
- 未读通知行:加粗(`font-semibold`)

**效果**:
- 过渡:同 R2
- Toast:从右下角滑入

### 渲染核对期望

**结构**:
- 通知列表:Table 列宽自适应,操作列固定宽度 150px
- 通知设置:卡片宽度自适应

**Token**:同 R2

**状态**:同 R2

**效果**:同 R2

### 设计还原风险清单

(无设计稿,按 DESIGN.md + shadcn/ui New York 规范实现)

## 完成判据

### 功能

1. ✅ 用户能查看通知列表(分类 Tab + 未读过滤)
2. ✅ 用户能标记已读/删除通知
3. ✅ 用户能查看未读计数(导航栏铃铛红点)
4. ✅ 用户能绑定钉钉 webhook + 测试
5. ✅ 项目 owner 能配置项目钉钉群 webhook
6. ✅ 部署崩溃时,项目 owner + 部署操作人收到站内信 + 钉钉 + toast
7. ✅ 任务失败时,任务创建者收到站内信 + toast
8. ✅ 钉钉 webhook 失效时,连续失败 3 次后通知用户重新配置

### 异常场景

1. ✅ 钉钉 webhook 无效:测试时返回 `18001`
2. ✅ 钉钉 webhook 失效:连续失败 3 次后标记失效 + 通知用户
3. ✅ 实时 toast 关闭时:仅写站内信,不弹 toast

### UI

1. ✅ 通知中心:分类 Tab + 未读过滤 + Table + 分页器
2. ✅ 导航栏铃铛:未读红点 + 计数 + 下拉最近 5 条
3. ✅ 实时 toast:右下角弹窗,5s 自动消失
4. ✅ 通知设置:钉钉 webhook 输入框 + 启用 Switch + 测试按钮
5. ✅ 所有文案符合"文案清单"

## 测试验证逻辑

### 单元测试(后端)

- `test_send_notification_success`:发送通知成功(写 notifications 表)
- `test_send_notification_with_toast`:实时 toast 推送(WebSocket)
- `test_send_notification_with_dingtalk`:钉钉 webhook 调用成功(模拟 httpx)
- `test_dingtalk_webhook_fail_3_times`:连续失败 3 次标记失效
- `test_mark_read_success`:标记已读成功
- `test_unread_count`:未读计数正确

### 集成测试(前端)

- 查看通知列表 → 点某条 → 标记已读 + 跳转
- 绑定钉钉 webhook → 点测试 → 显示成功
- 收到实时 toast → 点 toast → 跳转

### 手动测试

- 触发部署崩溃,验证收到站内信 + 钉钉 + toast
- 触发任务失败,验证收到站内信 + toast
- 绑定钉钉 webhook,验证测试消息可达

## 实现进度

(rd-dev 维护)

- [ ] 数据模型(notifications / user_notification_settings 表 + projects 表加 dingtalk 字段)
- [ ] 接口(通知列表/标记已读/删除/未读计数/通知设置/测试钉钉)
- [ ] WebSocket 通知频道
- [ ] 钉钉 webhook 调用服务
- [ ] 前端页面(通知中心/通知设置/导航栏铃铛/实时 toast)
- [ ] 单元测试
- [ ] 集成测试
- [ ] 手动测试
