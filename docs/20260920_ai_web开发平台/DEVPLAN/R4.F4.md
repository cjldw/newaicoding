# R4.F4 — 任务工作台照抄重写 vp #/t/T-301(BUG-UI-066 二修)

> 来源:用户指令 2026-09-23("完成任务页面的所有需求 UI,使用 vp #/t/T-301;你完全不按这个 ui 来")
> 状态:✅ fixed(2026-09-23 rd-fix 第 12 轮;并排视觉核对 + 功能扫测通过,tsc+build 零错;核对见 `.scratch/R4.F4/ui-check.md`)
> 证据:vp 基准 `vp-T301-baseline.png` vs React 现状 `react-task-before.png`(1600×900 并排,repo 根目录)
> 一手 vp 源码:`docs/20260920_ai_web开发平台/vp/index.html`
> - 骨架 `pageTask` L1458-1483;中栏 `centerPane` L1253-1377(requirement 分支 L1364-1376 / dev running L1254-1295 / dev pending L1296-1298 / test L1299-1330 / release L1331-1362)
> - 右栏 `rightPane` L1433-1456;`chatPane` L1408-1416;`actPane` L1427-1432;`treePane` L1220-1244;`termShell` L1147-1149
> - 徽章/字 chips:`tpB/stB/brChip` L767-770;TYPE/ST 表 L754-766;数据样例 T-301 L533、POLISH_CHAT L1039、ACT_301 L1400
> - CSS 原值:vp `<style>` 内 `.page.wide` L79、`.icon-btn` L98、`.chip` L110、`.chip-row` L112、`.twrap/.tabs/.tab/.pane` L135-144、`.wb-head` L326-328、`.wb/.col-*/.wb-mid` L329-337、响应式 L387-391;chat/act/term/tree/ed/pv 等类同段自行检索

## 描述(即需求)

React 任务工作台 `/tasks/:taskId` **逐元素照抄** vp `#/t/T-301`(及其四种类型变体),消灭"浮动卡片布局":

1. **页面骨架**:`.page.wide`(max-width:none、padding-bottom:0、flex column)→ `.wb-head` → `.wb` grid。禁止任何圆角卡片浮动壳
2. **wb-head**(vp L1466-1476):`btn btn-ghost icon-btn` 返回(← 回需求/项目)+ `.ttl`(类型徽章 tpB + `{短id} · {title}` + 状态徽章 stB)+ `.chip-row`(分支 brChip、容器 `box` icon chip、Runner `server` icon chip、lastCommit 有则显)+ `.acts`(按 vp:running=停止任务 btn-danger;test=驳回回开发+创建发布任务;release=打开:端口+下线;其余空)
3. **.wb 三栏 grid**(vp L1478-1481):`grid-template-columns:236px minmax(0,1fr) 384px`;col-tree 仅 dev 且非 pending(照抄 vp L1465 条件,含 treePane:全部文件/变更文件 ttab + tree + tree-foot);col-center = `section.col-center.wb-mid` 包 centerPane;col-right = `section.col-right` 包 rightPane。**DOM 顺序照抄**(非 dev 任务无 tree 节点,中栏自然落入 236px 列 = vp T-301 真实渲染,不许"修正")
4. **中栏 centerPane 按类型五分支**(全部照抄,Tab 条 `padding:0 10px;background:var(--surface)`,twrap 卡片化样式 `border-radius:0;border:none;box-shadow:none`):
   - requirement:PRD 草稿(容器内)/ 工作区 两 Tab
   - dev+running:编辑器/Diff(计数)/预览(HMR 徽章位)三 Tab + 状态栏(保留 R4.F1/F2 保存/全屏/导出,挂进 card-foot 位)
   - dev+pending:`card-body empty`「任务排队中…」空态
   - test:用例(计数)/测试报告/Diff;release:部署日志/配置/变更
5. **右栏恒三 Tab**:对话/终端/活动(vp L1433-1456);running 时终端 Tab 带 pulse dot;pane 切换按 vp `wtab` 语义(tab on ⇔ pane on,初始 chat 同步显示);终端 Tab 内为 termShell(term-head 工具条 + term-body 挂 xterm,保留 R4.F2 WS 与多 tab 能力)
6. **面包屑**(vp L1482):`项目 / {项目名} / {req 标识} / 任务 {短id}`,接 BreadcrumbOverrideProvider 真实数据,父级可点击
7. **CSS 原值移植**:vp `<style>` 相关类原类名原值进 globals.css(已存在的逐值核对校正,缺的补);类名冲突处 vp 赢

## 实现要点

- **禁止 DESIGN.md/shadcn 语义转译**:颜色/间距/字号一律抄 vp 原值(--zinc-*、--surface-2、13px、8px 13px 等)
- 数据接真实 API:任务/消息/活动/文件树/终端 WS;vp 内静态演示数据(POLISH_CHAT 等)只作 DOM 结构参照,**内容不照抄**
- R4.F1/F2 能力全保留:面板全屏(CSS 提升覆盖层)、保存/自动保存、导出会话日志、终端多 tab、@ 补全、错误 toast;挂载点迁入新骨架对应容器
- Tab 语义同步:React state 控制 tab on + pane 渲染,初始 'chat';不复制 vp 演示稿 chat pane 漏 `on` 类的 bug(记录于下)
- vp 已知演示怪癖照抄/不照抄清单:① 非 dev 无 tree → 中栏 236px 列 = **照抄**;② chat pane 初始缺 `on` 类右栏空白 = **不照抄**(同步显示);③ terminal pulse dot 仅 running = **照抄**

## 测试验证逻辑

1. `npx tsc --noEmit` + `npx vite build` 零错误
2. 静态核对 `.scratch/R4.F4/ui-check.md`:wb 骨架/wb-head 五段/三栏 grid 值/twrap 零圆角零边框/Tab 条原值/五类型分支/右栏三 Tab,逐项与 vp 源码行号对照
3. **并排视觉核对(硬门禁,上轮失守项)**:Playwright 1600×900 下 vp `http://127.0.0.1:4173/index.html#/t/T-301` 与 React `/tasks/{id}` 同框截图比对;dev/running 另对 `#/t/T-310` 比对;版式(非内容)不一致即打回
4. 功能回归:全屏/保存/导出/终端 WS/@补全/拖拽不回归(R4.F1/F2 清单)
