# R4.F3 — 任务工作台版式对齐 vp #/t/T-301(BUG-UI-066)

> 来源:用户指令 2026-09-23(使用 vp/index.html#/t/T-301 这样式)
> 状态:✅ fixed(2026-09-23;Playwright 实测结构断言+终端 Tab 全过,ui-check 见 .scratch/R4.F3/)
> 设计规范:`.scratch/design-spec/inner-task-workspace.md`(§2 pageTask 骨架 / §3 centerPane / §4 rightPane)

## 描述(即需求)

React 任务工作台版式与 vp 原型 `#/t/T-301` 对齐:
1. **wb-head 页头**(替代现有 border 任务头):返回 icon-btn + `.ttl`(类型徽章 + `{id} · {title}` + 状态徽章)+ `.chip-row`(分支/容器/Runner/最后提交)+ `.acts` 操作按钮组(停止/重试/查看 Diff/用例审阅/测试报告,按状态)
2. **三栏 `.wb` grid**:`236px | minmax(0,1fr) | 384px`(col-tree 仅 dev 且非 pending 显示;保留 R4.F2 的拖拽能力,初始宽对齐 vp)
3. **右栏改 Tab 组**:「对话 / 终端 / 活动」三个 Tab(替代 chat 顶置 + 活动流 + 终端底条的堆叠);终端从 200px 底条变为 Tab 内全高
4. **中栏 twrap 卡片化**:Tab 栏 + 内容区包进卡片容器(对齐 vp twrap card);保留状态栏/全屏/保存/导出(R4.F1/F2 能力全保留)

## 实现要点

- TaskDetail.tsx 结构重排,新增 `rightTab` state('chat'|'terminal'|'act');面板全屏/导出/保存逻辑不变,仅换挂载位置
- 全屏与右栏 Tab 互斥处理:面板全屏时 CSS 提升覆盖层,Tab 状态保留
- vp 类(.wb/.wb-head/.chip/.twrap/.tabs/.tab)已在 globals.css;缺的按 vp <style> 补
- 活动流 Terminal 高度 sash 移除(终端全高);左右栏拖拽保留

## 测试验证逻辑

1. 静态:wb-head 结构/三栏宽/右栏三 Tab/中栏卡片化,ui-check 落 `.scratch/R4.F3/`
2. `npx tsc --noEmit` + `npx vite build` 零错误
3. 浏览器:三 Tab 切换、全屏、导出、保存、拖拽回归
