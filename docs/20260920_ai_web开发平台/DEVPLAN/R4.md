# R4:任务(统一执行单元)

> 状态:⬜ 未开始
> 模块:M4 流程引擎
> 依赖:R3、R8、R13、R16

## PRD 引用

### 边界定义

- **做什么**:
  - 四种任务类型:**requirement(打磨)/ dev(开发)/ test(测试)/ release(发布)**,通过 `type` 区分
  - 任务创建时**拉起专属容器**,容器内 `git clone` + `checkout` 到指定分支
  - AI 在容器内执行(读写文件/跑命令/git 操作)
  - **任务对话框支持上传文件**(附件按钮 + 拖拽),文件上传到容器 `/tmp/uploads/{task_id}/`
  - **AI 引用文件**:用户在对话框输入 `@filename.txt`,平台自动补全当前任务已上传的文件;`< 100 KB` 的文件**自动注入内容到 prompt**,`≥ 100 KB` 的文件**只给路径**,AI 用工具(read_file)自己读
  - 任务结束(成功/失败/取消/超时)**销毁容器**(上传的文件随之丢失)
  - 任务产物(代码/文档)**全部通过 git commit + push 持久化到 GitLab**
- **不做什么**:
  - 任务指派到人(谁创建谁负责)
  - 任务子任务拆解(在 R3 需求打磨阶段定义)
  - 任务模板市场/编排 DAG
  - 上传文件持久化到 GitLab(临时文件;若需保留,引导用户让 AI 把内容写入正式文件并 commit)
  - 文件类型白名单(不限制,AI 自行判断;但禁止上传 > 50MB 的文件)

### 字段定义

(Task 主表)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| task_id | uuid | 是 | auto | |
| req_id | ref(R3) | 是 | - | 所属需求 |
| project_id | ref(R2) | 是 | - | 冗余便于查询 |
| type | enum | 是 | - | requirement / dev / test / release |
| title | string(128) | 是 | - | |
| description | text | 是 | - | 本次要做什么(AI 的输入) |
| base_branch | string(64) | 是 | =req.req_branch | **基础分支**(从哪里切/直接在哪个分支工作) |
| work_branch | string(64) | 是 | =base_branch | **工作分支**(默认=基础分支;特殊情况可新建) |
| status | enum | 是 | pending | pending/running/done/failed/cancelled/timeout |
| container_id | string | 否 | null | 任务运行时的 docker id |
| runner_id | ref(R16) | 否 | null | **任务运行的 Runner** |
| conversation_id | uuid | 是 | auto | 关联 Claude 会话 |
| created_by | ref(R1) | 是 | - | |
| started_at / finished_at | datetime | 否 | - | |
| total_tokens_in / total_tokens_out | int | 是 | 0 | |
| error_message | text | 否 | null | failed/timeout 时填 |
| last_commit_sha | string(40) | 否 | null | 任务结束时最后 commit |

(TaskUploadedFile 表)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| file_id | uuid | 是 | auto | |
| task_id | ref(R4) | 是 | - | |
| filename | string(255) | 是 | - | 原始文件名 |
| stored_filename | string(255) | 是 | - | 容器内实际文件名(冲突自动重命名,如 `file-1.txt`) |
| size | int | 是 | - | 字节 |
| mime_type | string(64) | 是 | - | |
| container_path | string(255) | 是 | `/tmp/uploads/{task_id}/{stored_filename}` | |
| uploaded_by | ref(R1) | 是 | - | |
| uploaded_at | datetime | 是 | now | |

(TaskMessage 表)
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| message_id | uuid | 是 | auto | |
| task_id | ref(R4) | 是 | - | |
| role | enum | 是 | - | user / assistant / tool |
| content | text | 是 | - | Markdown;`@filename` 在前端渲染为可点击链接 |
| file_refs | json | 否 | null | `[{file_id, filename, container_path, injected: bool}]` |
| tool_calls | json | 否 | null | `[{name, args, result, duration_ms}]` |
| tokens_in / tokens_out | int | 否 | 0 | |
| created_at | datetime | 是 | now | |

### 交互规则

| 场景/条件 | 行为/规则 | 结果/去向 |
|---|---|---|
| 创建任务 | 校验需求状态合法(如 dev 要求需求 approved);**校验创建者已绑定 GitLab token 且对所需 repo 有 read+write 权限**;**调度器选择 Runner**(R16);Runner 上拉起容器;**clone 主仓库到 `/workspace/main`** + **clone 所有 role=test 仓库到 `/workspace/tests/{repo_slug}`** + **clone role=docs/other 仓库到 `/workspace/{role}/{repo_slug}`**;每个仓库都 `checkout base_branch` + `git checkout -b work_branch`(若 ≠ base);启动 Claude 会话 | running |
| AI 执行中 | 文件变更/命令执行/工具调用 实时推送到工作台;AI 工作目录默认 `/workspace/main`(主仓库),可跨目录访问其他仓库 | 用户可见 |
| **上传文件** | 用户在任务对话框**点附件按钮或拖拽** → 前端上传到平台 → 平台转发到容器 `/tmp/uploads/{task_id}/`(同名自动重命名为 `{name}-1.txt`)→ 写 `task_uploaded_files` 表 → 前端显示附件列表 | 文件出现在对话框下方 |
| **上传限制** | 单文件 ≤ 50MB;单次 ≤ 10 个;单任务累计 ≤ 200MB;超限拒绝 | 前端提示 |
| **`@文件` 引用** | 用户在对话框输入 `@` → 前端自动补全当前任务已上传的文件名;选中后插入 `@filename`;发送时平台解析 `@filename` → 查 `task_uploaded_files` → **< 100KB 注入内容到 prompt** + **≥ 100KB 只注入路径**;`file_refs` 记录引用关系 | AI 收到文件内容或路径 |
| **下载文件** | 用户点击对话框/文件列表中的文件名 → 平台从容器拉取文件流回前端 | 浏览器下载 |
| AI 完成一段工作 | **对每个有改动的仓库**:`cd /workspace/{path} && git add -A && git commit -m "[ai:{type}] {task_title}"`(commit author = 创建任务的用户 GitLab username/email) | 分支有新提交 |
| AI push | **对每个有改动的仓库**:`git push origin {work_branch}`(用**用户 token**) | 同步到 GitLab,GitLab 侧显示该用户 push |
| push 冲突(他人在先) | AI 自动 `git pull --rebase` 或 `git merge`,解决冲突后再 push;失败则通知用户人工介入(在任务终端) | 或 failed |
| 用户手动停止 | Claude 中断 → 通知 Runner 销毁容器 | cancelled(未 push 的改动丢失) |
| 任务完成 | 最后一次 commit + push(**所有有改动的仓库**)→ 标记 done → 通知 Runner 销毁容器 | done |
| 任务失败/超时 | 标记失败原因 → 通知 Runner 销毁容器 | failed/timeout |
| 销毁容器前检查 | 若任一仓库的 work_branch 有未 push 的 commit → **强制 push**(用**用户 token**) | |
| **用户 token 失效(401)** | 任务标记 failed,error_message 提示"GitLab token 失效,请重新绑定";容器销毁 | 引导到 R1 个人设置 |
| **Runner 离线** | Runner 心跳超时(60s)→ 标记 Runner offline;其上的 running 任务**不动**(容器还在跑,只是平台无法通信);Runner 恢复后重新上报容器状态 | Runner 列表显示 offline |
| **Runner 上的任务超时** | 任务超时销毁兜底(60 分钟),即使 Runner offline,Runner 恢复后也会收到销毁指令 | |

### 异常与边界场景

- 单任务最长执行 **60 分钟**,超时自动 cancel(销毁前强制 push)
- 单项目**并发 running 任务 ≤ 3**(超出排队)
- 同需求分支并行任务 push 冲突:AI 自动 rebase;冲突文件超过 5 个或冲突复杂 → 任务 failed,提示人工
- 分支命名冲突:自动追加 `-{short_uuid}`
- 容器异常崩溃:任务标记 failed,Runner 自动清理;未 push 的改动丢失(因为没 commit)
- **用户 token 无效/权限不足**:创建任务时即拒绝,提示"请到个人设置绑定有效 GitLab token";任务执行中失效则任务 failed
- **Runner 全部离线**:任务创建时**排队等待**,直到有 Runner 上线;前端显示"等待可用 Runner"
- **文件上传失败**(容器不可达/磁盘满):前端提示"上传失败,请重试";任务不失败
- **同名文件冲突**:自动重命名 `file.txt` → `file-1.txt` → `file-2.txt`,在前端显示实际存储名

### 验收标准

1. 创建任务后容器 30s 内在某个 Runner 上拉起
2. AI 修改的文件能通过 git log 在 GitLab 看到
3. push 冲突时 AI 能自动 rebase,失败能通知人工
4. 任务结束后容器被销毁
5. 销毁前未 push 的 commit 被强制 push
6. **文件上传成功,AI 能通过 `@filename` 读取文件内容**
7. **文件上传 ≥ 100KB 时,AI 收到的是路径而非内容**
8. **Runner offline 时任务排队,Runner 恢复后继续执行**

## 依赖

- R3(需求)
- R8(容器)
- R13(模型配置)
- R16(Runner)

## 涉及模块/文件

**后端**:
- `backend/app/models/task.py`(Task/TaskUploadedFile/TaskMessage 模型)
- `backend/app/schemas/task.py`(Pydantic 模型)
- `backend/app/api/tasks.py`(任务接口)
- `backend/app/services/task_service.py`(任务业务逻辑)
- `backend/app/services/claude_service.py`(Claude Agent SDK 调用,按 D3)
- `backend/app/services/file_upload_service.py`(文件上传业务逻辑)

**前端**:
- `frontend/src/pages/tasks/TaskCreate.tsx`(创建任务对话框)
- `frontend/src/pages/tasks/TaskDetail.tsx`(任务工作台)
- `frontend/src/components/TaskChat.tsx`(任务对话框,含文件上传 + @引用)
- `frontend/src/components/ActivityStream.tsx`(活动流面板,AI 工具调用事件)
- `frontend/src/api/tasks.ts`(API 客户端)

## 复用清单

**后端**:
- FastAPI `Depends(get_current_user)` 依赖注入
- SQLAlchemy 2.0 async session
- `claude-agent-sdk`(Python 版,按 D3)
- Runner 通信(R16)
- `httpx`(async)

**前端**:
- shadcn/ui 组件:`Button`, `Input`, `Card`, `Dialog`, `Textarea`, `Badge`, `Alert`
- `react-dropzone`(文件拖拽上传)
- react-query `useQuery` / `useMutation`
- lucide-react 图标:`Plus`, `Upload`, `Download`, `X`, `File`, `Paperclip`

## 实现要点

### 数据模型变更

**新增表 `tasks`**:

| 字段 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | BIGINT UNSIGNED | 是 | AUTO_INCREMENT | PRIMARY KEY | 内部 id |
| task_id | CHAR(36) | 是 | UUID | UNIQUE | 对外 id(uuid) |
| req_id | CHAR(36) | 是 | - | INDEX | 需求 id |
| project_id | CHAR(36) | 是 | - | INDEX | 项目 id |
| type | ENUM('requirement', 'dev', 'test', 'release') | 是 | - | INDEX | 任务类型 |
| title | VARCHAR(128) | 是 | - | - | 标题 |
| description | TEXT | 是 | - | - | 描述 |
| base_branch | VARCHAR(64) | 是 | - | - | 基础分支 |
| work_branch | VARCHAR(64) | 是 | - | - | 工作分支 |
| status | ENUM('pending', 'running', 'done', 'failed', 'cancelled', 'timeout') | 是 | 'pending' | INDEX | 状态 |
| container_id | VARCHAR(64) | 否 | NULL | INDEX | 容器 id |
| runner_id | CHAR(36) | 否 | NULL | INDEX | Runner id |
| conversation_id | CHAR(36) | 是 | UUID | - | Claude 会话 id |
| created_by | CHAR(36) | 是 | - | INDEX | 创建者 user_id |
| started_at | DATETIME | 否 | NULL | - | 开始时间 |
| finished_at | DATETIME | 否 | NULL | - | 完成时间 |
| total_tokens_in | INT | 是 | 0 | - | 输入 token 数 |
| total_tokens_out | INT | 是 | 0 | - | 输出 token 数 |
| error_message | TEXT | 否 | NULL | - | 错误信息 |
| last_commit_sha | VARCHAR(40) | 否 | NULL | - | 最后 commit sha |
| extended_attributes | JSON | 否 | NULL | - | 扩展属性(test_cases/deploy_port 等) |
| created_at | DATETIME | 是 | CURRENT_TIMESTAMP | - | 创建时间 |
| updated_at | DATETIME | 是 | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | - | 更新时间 |

**新增表 `task_uploaded_files`**:

| 字段 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | BIGINT UNSIGNED | 是 | AUTO_INCREMENT | PRIMARY KEY | 内部 id |
| file_id | CHAR(36) | 是 | UUID | UNIQUE | 对外 id(uuid) |
| task_id | CHAR(36) | 是 | - | INDEX | 任务 id |
| filename | VARCHAR(255) | 是 | - | - | 原始文件名 |
| stored_filename | VARCHAR(255) | 是 | - | - | 容器内实际文件名 |
| size | BIGINT | 是 | - | - | 字节 |
| mime_type | VARCHAR(64) | 是 | - | - | MIME 类型 |
| container_path | VARCHAR(255) | 是 | - | - | 容器内路径 |
| uploaded_by | CHAR(36) | 是 | - | - | 上传者 user_id |
| uploaded_at | DATETIME | 是 | CURRENT_TIMESTAMP | - | 上传时间 |

**新增表 `task_messages`**:

| 字段 | 类型 | 必填 | 默认值 | 索引 | 说明 |
|---|---|---|---|---|---|
| id | BIGINT UNSIGNED | 是 | AUTO_INCREMENT | PRIMARY KEY | 内部 id |
| message_id | CHAR(36) | 是 | UUID | UNIQUE | 对外 id(uuid) |
| task_id | CHAR(36) | 是 | - | INDEX | 任务 id |
| role | ENUM('user', 'assistant', 'tool') | 是 | - | - | 角色 |
| content | TEXT | 是 | - | - | 内容(Markdown) |
| file_refs | JSON | 否 | NULL | - | 文件引用 |
| tool_calls | JSON | 否 | NULL | - | 工具调用 |
| tokens_in | INT | 否 | 0 | - | 输入 token 数 |
| tokens_out | INT | 否 | 0 | - | 输出 token 数 |
| created_at | DATETIME | 是 | CURRENT_TIMESTAMP | - | 创建时间 |

**SQL 全文见 `DEPLOY.md`**

### 接口契约

#### 1. 任务列表

- **路径**:`GET /api/requirements/{req_id}/tasks`
- **鉴权**:JWT(项目成员)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "items": [
        {
          "task_id": "uuid",
          "type": "dev",
          "title": "string",
          "status": "running",
          "created_by": { /* 同需求列表 */ },
          "created_at": "datetime",
          "started_at": "datetime",
          "finished_at": null
        }
      ]
    },
    "message": "ok"
  }
  ```

#### 2. 创建任务

- **路径**:`POST /api/requirements/{req_id}/tasks`
- **鉴权**:JWT(owner/editor,需绑定 token)
- **请求**:
  ```json
  {
    "type": "dev|test|release",
    "title": "string(128)",
    "description": "string",
    "base_branch": "string(可选,默认=req.req_branch)",
    "work_branch": "string(可选,默认=base_branch)"
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "task_id": "uuid"
    },
    "message": "任务已创建"
  }
  ```
- **错误码**:
  - `4001`: 需求状态不合法(如 dev 要求需求 approved)
  - `4002`: 用户未绑定 GitLab token
  - `4003`: 单项目并发 running 任务超限(>3)

#### 3. 任务详情

- **路径**:`GET /api/tasks/{task_id}`
- **鉴权**:JWT(项目成员)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "task_id": "uuid",
      "type": "dev",
      "title": "string",
      "description": "string",
      "status": "running",
      "base_branch": "req-xxx",
      "work_branch": "req-xxx",
      "container_id": "docker_id",
      "runner_id": "uuid",
      "created_by": { /* 同列表 */ },
      "started_at": "datetime",
      "finished_at": null,
      "total_tokens_in": 1234,
      "total_tokens_out": 5678,
      "error_message": null,
      "last_commit_sha": null
    },
    "message": "ok"
  }
  ```

#### 4. 停止任务

- **路径**:`POST /api/tasks/{task_id}/stop`
- **鉴权**:JWT(owner/editor)
- **响应**:
  ```json
  {
    "code": 0,
    "message": "任务已停止"
  }
  ```

#### 5. 重试任务

- **路径**:`POST /api/tasks/{task_id}/retry`
- **鉴权**:JWT(owner/editor)
- **响应**:
  ```json
  {
    "code": 0,
    "message": "任务已重试"
  }
  ```

#### 6. 任务对话历史

- **路径**:`GET /api/tasks/{task_id}/messages`
- **鉴权**:JWT(项目成员)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "items": [
        {
          "message_id": "uuid",
          "role": "user",
          "content": "string",
          "file_refs": [{"file_id": "uuid", "filename": "a.txt", "container_path": "/tmp/uploads/...", "injected": true}],
          "tool_calls": null,
          "tokens_in": 100,
          "tokens_out": 0,
          "created_at": "datetime"
        }
      ]
    },
    "message": "ok"
  }
  ```

#### 7. 发送消息

- **路径**:`POST /api/tasks/{task_id}/messages`
- **鉴权**:JWT(owner/editor)
- **请求**:
  ```json
  {
    "content": "string(支持 @filename 引用)"
  }
  ```
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "message_id": "uuid"
    },
    "message": "ok"
  }
  ```

#### 8. 上传文件

- **路径**:`POST /api/tasks/{task_id}/files/upload`
- **鉴权**:JWT(owner/editor)
- **请求**:multipart/form-data
  - `file`: 文件(单文件 ≤ 50MB)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "file_id": "uuid",
      "filename": "a.txt",
      "stored_filename": "a.txt",
      "size": 1234,
      "container_path": "/tmp/uploads/{task_id}/a.txt"
    },
    "message": "上传成功"
  }
  ```
- **错误码**:
  - `4004`: 文件超限(>50MB)
  - `4005`: 单次上传文件数超限(>10)
  - `4006`: 单任务累计上传超限(>200MB)

#### 9. 列出已上传文件

- **路径**:`GET /api/tasks/{task_id}/files/uploads`
- **鉴权**:JWT(项目成员)
- **响应**:
  ```json
  {
    "code": 0,
    "data": {
      "items": [
        {
          "file_id": "uuid",
          "filename": "a.txt",
          "stored_filename": "a.txt",
          "size": 1234,
          "uploaded_by": { /* 同需求列表 */ },
          "uploaded_at": "datetime"
        }
      ]
    },
    "message": "ok"
  }
  ```

#### 10. 下载文件

- **路径**:`GET /api/tasks/{task_id}/files/uploads/{file_id}`
- **鉴权**:JWT(项目成员)
- **响应**:文件流

#### 11. 删除文件

- **路径**:`DELETE /api/tasks/{task_id}/files/uploads/{file_id}`
- **鉴权**:JWT(owner/editor)
- **响应**:
  ```json
  {
    "code": 0,
    "message": "已删除"
  }
  ```

#### WebSocket 任务事件流频道

- **路径**:`/ws/tasks/{task_id}/events`
- **鉴权**:JWT(项目成员)
- **消息协议**:
  - 平台 → 前端:
    ```json
    {"type": "tool_call", "name": "write_file", "args": {...}, "result": "...", "duration_ms": 123}
    {"type": "status_changed", "status": "done"}
    ```

### 权限矩阵

| 操作 | owner | editor | viewer |
|---|---|---|---|
| 查看任务列表/详情/对话历史 | ✅ | ✅ | ✅ |
| 创建任务 | ✅(需绑定 token) | ✅(需绑定 token) | ❌ |
| 停止/重试任务 | ✅ | ✅ | ❌ |
| 发送消息 | ✅ | ✅ | ❌ |
| 上传/下载文件 | ✅ | ✅ | ✅(仅下载) |
| 删除文件 | ✅ | ✅ | ❌ |

### 服务端行为规格

#### 创建任务

1. 校验需求状态合法(如 dev 要求需求 approved)
2. 校验用户已绑定 GitLab token 且对所需 repo 有 read+write 权限(调 GitLab API)
3. 校验单项目并发 running 任务 ≤ 3
4. 调度器选 Runner(最少负载 + role 匹配,R16)
5. 平台通过 Runner WebSocket 发送 `start_container` 消息(R8)
6. Runner 拉起容器后回报 `container_started`
7. 平台写 `tasks` 表 + `containers` 表
8. 启动 Claude 会话(按 D3):
   ```python
   from claude_agent_sdk import ClaudeSDKClient

   client = ClaudeSDKClient(
       base_url=model_config.base_url,
       api_key=decrypted_api_key,
       model=model_config.model
   )
   conversation = await client.create_conversation()
   ```
9. 平台通过 WebSocket 向 Runner 发送启动 Claude 会话指令:
   ```json
   {
     "type": "start_claude_session",
     "container_id": "docker_id",
     "conversation_id": "uuid"
   }
   ```
10. Runner 在容器内启动 Claude CLI(后台进程)

#### 发送消息

1. 用户发送消息(可含 `@filename` 引用)
2. 平台解析 `@filename` → 查 `task_uploaded_files` 表
3. **< 100KB 文件**:读取内容注入 prompt:
   ```
   用户消息原文

   ---
   File: {filename}
   ```
   {content}
   ```
   ---
   ```
4. **≥ 100KB 文件**:只注入路径:
   ```
   用户消息原文

   ---
   File: {filename} (size: {size} bytes)
   Path: {container_path}
   ---
   ```
5. 平台调 Claude SDK `conversation.send_message(prompt)`
6. Claude 返回响应 → 平台写 `task_messages` 表
7. 平台通过 WebSocket 推送工具调用事件到前端(活动流)

#### AI 执行工具(写文件/跑命令/git)

1. Claude SDK 返回工具调用事件(如 `write_file`)
2. 平台通过 WebSocket 向 Runner 发送指令:
   ```json
   {
     "type": "exec_tool",
     "container_id": "docker_id",
     "tool": "write_file",
     "args": {"path": "src/main.py", "content": "..."}
   }
   ```
3. Runner 在容器内执行 → 回报结果
4. 平台写 `task_messages` 表(role=tool)
5. 平台通过 WebSocket 推送事件到前端(活动流)

#### AI 完成一段工作(commit + push)

1. Claude SDK 返回"完成"信号
2. 平台通过 Runner 在容器内执行:
   ```bash
   cd /workspace/main
   git add -A
   git commit -m "[ai:{type}] {task_title}"
   git push origin {work_branch}
   ```
3. 若有多个仓库改动,对每个仓库都执行
4. 平台更新 `tasks` 表(last_commit_sha)

#### 停止任务

1. 平台调 Claude SDK `conversation.cancel()`
2. 平台通过 Runner 销毁容器(R8)
3. 平台更新 `tasks` 表(status=cancelled)

### 页面结构说明

#### 创建任务对话框

- **区块结构**:
  1. **对话框标题**:"创建开发任务"(根据 type 变化)
  2. **表单**:
     - 标题输入框(label="标题",placeholder="如:实现用户登录功能")
     - 描述输入框(label="描述",textarea,placeholder="本次要让 AI 做什么?")
     - 基础分支输入框(label="基础分支",placeholder="默认=需求分支",可选)
     - 工作分支输入框(label="工作分支",placeholder="默认=基础分支",可选)
  3. **按钮区**:"取消" + "创建"

#### 任务工作台 `/tasks/{task_id}`

- **区块结构**:
  1. **任务头**:标题 + 类型徽章 + 状态徽章 + 操作按钮(停止/重试)
  2. **左侧**:文件树(R11)
  3. **中间**:
     - **上半**:Monaco 编辑器(R11)
     - **下半**:Diff 视图 / 预览 iframe(R10)
  4. **右侧**:
     - **上半**:任务对话框(消息列表 + 输入框 + 附件按钮)
     - **下半**:活动流面板(AI 工具调用事件)+ 终端 Tab(R9)

### 页面交互流程

#### 创建任务流程

1. 用户在需求详情页点"创建开发任务" → 打开对话框
2. 填表 → 点"创建"
3. 调 `POST /api/requirements/{req_id}/tasks`
4. 成功 → 跳转 `/tasks/{task_id}`

#### 发送消息流程

1. 用户在任务对话框输入消息(可含 `@filename`)
2. 输入 `@` 时,前端调 `GET /api/tasks/{tid}/files/uploads` 获取已上传文件列表,自动补全
3. 选中文件后插入 `@filename`
4. 点"发送" → 调 `POST /api/tasks/{tid}/messages`
5. 前端显示用户消息 → Claude 响应流式返回 → 前端渲染

#### 上传文件流程

1. 用户点附件按钮或拖拽文件到对话框
2. 前端调 `POST /api/tasks/{tid}/files/upload`
3. 成功后文件出现在对话框下方附件列表
4. 用户点击文件名 → 下载文件

### 控件联动

| 触发控件 | 事件 | 响应行为 |
|---|---|---|
| 任务对话框输入框 | 输入 `@` | 自动补全已上传文件 |
| 附件按钮 | 点击 | 打开文件选择器 |
| 拖拽文件到对话框 | drop | 上传文件 |
| 附件列表文件名 | 点击 | 下载文件 |

### 文案清单

| 场景 | 文案 |
|---|---|
| 创建任务对话框标题 | 创建开发任务 / 创建测试任务 / 创建发布任务 |
| 标题 label | 标题 |
| 描述 label | 描述 |
| 基础分支 label | 基础分支 |
| 工作分支 label | 工作分支 |
| 创建按钮 | 创建 |
| 停止按钮 | 停止 |
| 重试按钮 | 重试 |
| 发送按钮 | 发送 |
| 附件按钮提示 | 上传文件 |
| 状态徽章-pending | 等待中 |
| 状态徽章-running | 运行中 |
| 状态徽章-done | 已完成 |
| 状态徽章-failed | 失败 |
| 状态徽章-cancelled | 已取消 |
| 状态徽章-timeout | 超时 |
| 类型徽章-requirement | 需求打磨 |
| 类型徽章-dev | 开发 |
| 类型徽章-test | 测试 |
| 类型徽章-release | 发布 |
| 错误提示-需求状态不合法 | 需求状态不允许创建该类型任务 |
| 错误提示-未绑定 token | 请到个人设置绑定 GitLab token |
| 错误提示-并发超限 | 项目并发任务数已达上限(3个) |
| 错误提示-文件超限 | 文件大小超过 50MB |
| 错误提示-单次上传超限 | 单次最多上传 10 个文件 |
| 错误提示-累计上传超限 | 单任务累计上传超过 200MB |
| 成功提示-任务已创建 | 任务已创建 |
| 成功提示-任务已停止 | 任务已停止 |
| 成功提示-任务已重试 | 任务已重试 |
| 成功提示-上传成功 | 上传成功 |
| 成功提示-已删除 | 已删除 |

### 设计规范

**Token**:同 R1

**布局**:
- 任务工作台:三栏布局(左侧文件树 250px + 中间编辑器自适应 + 右侧对话框 400px)

**组件**:
- Dialog:同 R2
- Badge:pending=outline/running=primary/done=success/failed=danger/cancelled=secondary/timeout=danger;requirement=secondary/dev=primary/test=primary/release=success
- Textarea:shadcn/ui `Textarea` 组件

**状态**:
- 按钮/输入框:同 R1

**效果**:
- 过渡:同 R1
- Dialog:同 R2

### 渲染核对期望

**结构**:
- 任务工作台:三栏布局,左侧文件树固定 250px,右侧对话框固定 400px,中间自适应

**Token**:同 R1

**状态**:同 R1

**效果**:同 R1

### 设计还原风险清单

(无设计稿,按 DESIGN.md + shadcn/ui New York 规范实现)

## 完成判据

### 功能

1. ✅ 用户能创建任务(dev/test/release)
2. ✅ 创建任务时校验需求状态合法 + 用户已绑定 GitLab token + 并发不超限
3. ✅ 任务创建后容器在某个 Runner 上拉起
4. ✅ 用户能在任务对话框发送消息(含 `@filename` 引用)
5. ✅ 文件上传成功,AI 能通过 `@filename` 读取文件内容(< 100KB 注入内容,≥ 100KB 只给路径)
6. ✅ AI 修改的文件能 commit + push 到 GitLab(用用户 token)
7. ✅ push 冲突时 AI 能自动 rebase,失败能通知人工
8. ✅ 任务结束后容器被销毁
9. ✅ 销毁前未 push 的 commit 被强制 push
10. ✅ 用户能停止/重试任务

### 异常场景

1. ✅ 需求状态不合法:返回 `4001`
2. ✅ 用户未绑定 token:返回 `4002`
3. ✅ 并发超限:返回 `4003`
4. ✅ 文件超限:返回 `4004` / `4005` / `4006`
5. ✅ 用户 token 失效:任务 failed,提示重新绑定
6. ✅ Runner offline:任务排队,Runner 恢复后继续

### UI

1. ✅ 任务工作台:三栏布局(文件树 + 编辑器 + 对话框)
2. ✅ 任务对话框:支持 `@` 引用自动补全 + 附件上传 + 附件列表
3. ✅ 活动流面板:显示 AI 工具调用事件
4. ✅ 所有文案符合"文案清单"

## 测试验证逻辑

### 单元测试(后端)

- `test_create_task_success`:创建任务成功(模拟 Runner)
- `test_create_task_invalid_req_status`:需求状态不合法返回 4001
- `test_create_task_no_gitlab_token`:未绑定 token 返回 4002
- `test_create_task_concurrent_limit`:并发超限返回 4003
- `test_upload_file_success`:上传文件成功
- `test_upload_file_size_limit`:文件超限返回 4004
- `test_send_message_with_file_ref`:发送消息含 `@filename`,验证 file_refs 记录
- `test_ai_commit_push`:AI 完成工作后能 commit + push

### 集成测试(前端)

- 创建任务流程:填表 → 点创建 → 跳转任务工作台
- 发送消息流程:输入消息 → 点发送 → 显示 Claude 响应
- 上传文件流程:点附件按钮 → 选文件 → 上传成功

### 手动测试

- 创建任务后,验证容器在 Runner 上拉起
- 在任务对话框发送消息,验证 Claude 响应
- 上传文件,验证 AI 能读取文件内容
- 验证 AI 修改的文件能 commit + push 到 GitLab

## 实现进度

(rd-dev 维护)

- [ ] 数据模型(tasks / task_uploaded_files / task_messages 表)
- [ ] 接口(任务 CRUD/消息/文件上传/下载/删除)
- [ ] Claude Agent SDK 集成
- [ ] 前端页面(创建任务对话框/任务工作台/任务对话框/活动流面板)
- [ ] 单元测试
- [ ] 集成测试
- [ ] 手动测试
