# R7:发布任务(type=release)

> 状态:⬜ 未开始
> 模块:M4 流程引擎
> 依赖:R6、R4、R15

## PRD 引用

### 边界定义

- **做什么**:
  - 创建发布任务 → 容器内 `git checkout master && git merge req-{reqId}` → 冲突 AI 解决,失败任务 failed
  - 执行**发布脚本**(项目级配置,如 `npm run build && pm2 reload`,或平台默认:在容器内启动服务)
  - **生成对外地址**:`http://{slug}.coding-console.zhanqitv.com.cn:{port}`(HTTP,端口用户自定义)
  - 部署产物(CHANGELOG / deploy-log)**commit 到 master** 的 `docs/releases/{taskId}/`
  - 发布后**需求状态推进**,全部部署完成 → 需求 done → 触发归档
- **不做什么(V1)**:
  - HTTPS(端口直连)
  - 多环境(staging/prod)
  - 自定义域名
  - 外部服务器部署(SSH/K8s)
  - 回滚(失败就走驳回)
  - 蓝绿/金丝雀

### 字段定义

沿用 R4 通用字段;特定字段:
| 字段 | 类型 | 必填 | 默认值 | 说明 |
|---|---|---|---|---|
| target_env | enum | 是 | subdomain | V1 仅 subdomain |
| deploy_url | string | 是 | `http://{slug}.coding-console.zhanqitv.com.cn:{port}` | |
| deploy_port | int | 是 | - | 用户自定义,范围 10000-10099 |
| deploy_script | text | 是 | 项目默认 | 用户可改 |
| deploy_log_path | string | 是 | `docs/releases/{taskId}/deploy-log.txt` | |
| merge_commit_sha | string(40) | 否 | null | merge 到 master 的 commit |

### 交互规则

| 场景/条件 | 行为/规则 | 结果/去向 |
|---|---|---|
| 创建发布任务 | 用户填 deploy_port(平台检测冲突)→ 拉起容器 → checkout master → **merge req-{reqId}(容器内执行,但 merge commit 通过 GitLab API 用平台 bot token 创建,确保系统动作可追溯)** | deploying |
| merge 冲突 | AI 自动解决(在容器内 merge + commit + push 用 bot token)→ 仍冲突 → failed,提示人工在终端介入 | |
| 部署脚本执行 | 容器内运行,日志实时推送到工作台 | |
| 健康检查 | `GET http://localhost:{port}/` 200(可配置路径)→ deployed | URL 可公开访问 |
| **部署产物 commit** | CHANGELOG.md + deploy-log.txt commit 到 master 的 `docs/releases/{taskId}/`(**用平台 bot token,系统动作**) | |
| 部署失败 | status=failed → 可驳回回测试(新建 test 任务)或重试 | |
| 需求下所有发布任务 deployed | 需求 → done → 触发 R14 归档 | |
| **容器不销毁** | **部署中的任务容器保持运行**(对外提供服务),直到"下线"或"项目归档" | 持续运行 |

### 异常与边界场景

- 端口冲突:平台检测 `deploy_port` 在**全平台**唯一,冲突拒绝
- 单项目**同时部署数 ≤ 5**
- 部署中的任务**不占用** R4 的"并发 running 任务 ≤ 3"配额
- 部署容器崩溃 → 告警到项目 owner **站内信 + 钉钉 webhook**(Q12 已确认)
- 下线 = 用户点击"下线" → 摘除路由 + 销毁容器

### 验收标准

1. 发布后 `http://{slug}.coding-console.zhanqitv.com.cn:{port}` 可公开访问
2. 部署产物 commit 到 master
3. 需求下所有发布完成后,需求自动 done 并触发归档
4. 下线后 URL 立即不可访问

## 依赖

- R6(测试任务)
- R4(任务)
- R15(网关)

## 涉及模块/文件

**后端**:
- `backend/app/services/task_service.py`(创建 release 任务逻辑,含 merge/部署/产物 commit)
- `backend/app/services/gitlab_service.py`(GitLab API 调用:merge/commit)

**前端**:
- `frontend/src/pages/tasks/TaskCreate.tsx`(创建任务对话框,type=release)
- `frontend/src/pages/tasks/DeployStatus.tsx`(部署状态页)

## 复用清单

(同 R4)

## 实现要点

### 数据模型变更

(沿用 R4 的 `tasks` 表,`extended_attributes` JSONB 字段存 `target_env` / `deploy_url` / `deploy_port` / `deploy_script` / `deploy_log_path` / `merge_commit_sha`)

### 接口契约

(沿用 R4 通用接口;特定接口:)

#### 1. 下线

- **路径**:`POST /api/tasks/{task_id}/offline`
- **鉴权**:JWT(owner/editor)
- **响应**:
  ```json
  {
    "code": 0,
    "message": "已下线"
  }
  ```

### 权限矩阵

(同 R4)

### 服务端行为规格

#### 创建发布任务

1. 校验需求下至少一个 test 任务 passed
2. 校验 deploy_port 全平台唯一(查 `tasks` 表的 `extended_attributes.deploy_port`)
3. 校验单项目同时部署数 ≤ 5
4. 创建任务(type=release),拉起容器在 **deploy Runner** 上(R16)
5. 容器内执行:
   ```bash
   cd /workspace/main
   git checkout master
   git merge req-{reqId}
   ```
6. merge 冲突 → AI 自动解决(在容器内 merge + commit + push 用 **bot token**)→ 仍冲突 → failed
7. 执行部署脚本(项目级配置或平台默认)
8. 健康检查:`GET http://localhost:{port}/` 200
9. 成功 → 注册路由(R15,upstream 指向 deploy Runner)→ status=deployed
10. 部署产物 commit 到 master(用 bot token):
    ```bash
    git add docs/releases/{taskId}/
    git commit -m "[ai:release] {task_title}"
    git push origin master
    ```
11. 容器**不销毁**(保持运行)

#### 下线

1. 用户点"下线"
2. 平台摘除路由(R15)
3. 平台通知 Runner 销毁容器
4. status=undeployed

### 页面结构说明

#### 部署状态页 `/tasks/{task_id}/deploy`

- **区块结构**:
  1. **页面标题**:"部署状态"
  2. **状态卡片**:部署 URL(可点击)/ 状态徽章(deploying=primary/deployed=success/failed=danger)/ 部署时间
  3. **部署日志**(Textarea,只读,等宽字体)
  4. **操作栏**(若 deployed):"下线"按钮(danger)

### 页面交互流程

#### 创建发布任务流程

1. 用户在需求详情页点"创建发布任务" → 打开对话框
2. 填 deploy_port(平台实时检测冲突)+ deploy_script(可选,默认项目配置)
3. 点"创建" → 调 `POST /api/requirements/{req_id}/tasks`
4. 成功 → 跳转部署状态页
5. 部署中 → 显示 deploying 状态 + 实时日志
6. 部署成功 → 显示 deployed 状态 + 部署 URL

#### 下线流程

1. 用户在部署状态页点"下线"
2. 弹出确认对话框:"确定下线吗?下线后 URL 将不可访问"
3. 点"确定" → 调 `POST /api/tasks/{tid}/offline`
4. 成功 → URL 不可访问

### 控件联动

| 触发控件 | 事件 | 响应行为 |
|---|---|---|
| deploy_port 输入框 | 输入 | 实时检测冲突(调 `GET /api/tasks/check-port?port=xxx`) |
| 部署 URL | 点击 | 新窗口打开 |

### 文案清单

| 场景 | 文案 |
|---|---|
| 创建发布任务对话框标题 | 创建发布任务 |
| deploy_port label | 部署端口 |
| deploy_script label | 部署脚本 |
| 部署状态页标题 | 部署状态 |
| 部署 URL label | 部署 URL |
| 状态徽章-deploying | 部署中 |
| 状态徽章-deployed | 已部署 |
| 状态徽章-failed | 部署失败 |
| 部署日志 label | 部署日志 |
| 下线按钮 | 下线 |
| 下线确认对话框 | 确定下线吗?下线后 URL 将不可访问 |
| 错误提示-端口冲突 | 端口已被占用 |
| 错误提示-部署数超限 | 项目同时部署数已达上限(5个) |
| 成功提示-已下线 | 已下线 |

### 设计规范

(同 R4)

### 渲染核对期望

(同 R4)

### 设计还原风险清单

(无)

## 完成判据

### 功能

1. ✅ 用户能创建发布任务(deploy_port 自定义)
2. ✅ 容器内能 merge req-{reqId} 到 master(冲突 AI 解决)
3. ✅ 部署脚本能执行
4. ✅ 健康检查通过后,部署 URL 可公开访问
5. ✅ 部署产物能 commit 到 master(用 bot token)
6. ✅ 用户能下线(URL 不可访问 + 容器销毁)
7. ✅ 需求下所有发布完成后,需求自动 done

### 异常场景

1. ✅ 端口冲突:返回 `7001`
2. ✅ 部署数超限:返回 `7002`
3. ✅ merge 冲突 AI 解决不了:任务 failed,提示人工介入
4. ✅ 部署容器崩溃:告警到项目 owner **站内信 + 钉钉 webhook**

### UI

1. ✅ 部署状态页:状态卡片 + 部署日志(只读)
2. ✅ 所有文案符合"文案清单"

## 测试验证逻辑

### 单元测试(后端)

- `test_create_release_task_success`:创建发布任务成功
- `test_create_release_task_port_conflict`:端口冲突返回 7001
- `test_create_release_task_limit_exceeded`:部署数超限返回 7002
- `test_merge_conflict_ai_resolve`:merge 冲突 AI 能自动解决
- `test_offline_success`:下线成功

### 集成测试(前端)

- 创建发布任务流程:填端口 → 点创建 → 跳转部署状态页
- 下线流程:点下线 → 确认 → URL 不可访问

### 手动测试

- 创建发布任务,验证部署 URL 可公开访问
- 验证部署产物 commit 到 master
- 下线后验证 URL 不可访问

## 实现进度

(rd-dev 维护)

- [ ] 接口(下线)
- [ ] 前端页面(部署状态页)
- [ ] 单元测试
- [ ] 集成测试
- [ ] 手动测试
