# 复核报表:需求关联用户/评审通知 + 原型链接/交付时间

> 日期:2026-09-26 | 轮次:首轮 | 环境:http://127.0.0.1:8000(dev,uvicorn --reload,worker 加载最新代码)| 断言脚本资产:`.scratch/check/{R}/assert.py`(回归轮直接重跑)

| 需求点 | 接口断言 | 业务旅程 | 判据抽查 | BUG |
|---|---|---|---|---|
| R1 关联用户字段 | 30/30 ✅(异常 5/5) | — | 5/5 ✅ | BUG-003(计划外,P3) |
| R2 关联用户详情可编辑 | 33/33 ✅ | J2 步骤覆盖 ✅ | 4/4 ✅ | - |
| R3 评审通过站内通知 | ⚠️ 环境阻塞(18/18 已跑全绿;4 组 approve 断言卡 polish 8003 无 Runner) | J2 端到端 9/10 ✅(approve→收信/排除操作人实证) | 5/5 ✅ | - |
| R4 原型链接 | 40/42(2 失败=BUG-001 规格缺口,非接口缺陷) | J1 前置创建链路 ✅(旅程整体环境中断) | 4/4 ✅ | BUG-001(轻微) |
| R5 交付时间+逾期标记 | 32/32 ✅ | — | 4/4 ✅ | - |
| R6 交付提醒巡检 | 无公开接口(设计如此) | J3 端到端 16/16 ✅(巡检→通知→防重入→清理回收) | 6/6 ✅ | - |
| R7 工作台「与我相关」 | 28/28 ✅ | — | 5/5 ✅ | BUG-002(轻微) |

**单测**:⚠️ 环境阻塞,全量数不可得(两轮)。已验证:单元片段 98 条 0 真实回归;R6 相关 20/20、R3 相关 6/6 于静默窗口全绿;已知历史失败 R34.F1(另一流程 WIP)未到达。重试日志:`.scratch/check/unit-test-retry.log`(残留连接 214742-45 需服务端 KILL,本机权限不足)

## 结论

- **接口级 bug:0**;**规格缺口:2**(BUG-001 创建响应不回显 prototype_links、BUG-002 recent 行缺 related 透传——均契约补齐类,流向 rd-plan);**计划外轻微缺陷:1**(BUG-003 `api/requirements.py:236` require_project_role 缺 await,现无越权洞)
- 7 需求点判据抽查 **33/33 ✅**;J2/J3 旅程把 R3 通知链、R6 巡检链在真实 API/DB 层端到端验证通过
- 引导:`/rd-fix` 处理 BUG-001/002/003(均轻微,可合并一轮);修复后回归轮只重跑受影响接口段

## 环境阻塞(不写 BUG,修复后回归轮重跑对应段)

| 项 | 现象 | 需要的动作 | 重跑 |
|---|---|---|---|
| R3 接口级 approve 断言 | polish 调度 code=8003 无可用 Runner(角色不匹配) | dev 环境补一台角色匹配的在线 Runner | `python .scratch/check/R3/assert.py` |
| J1 三字段创建旅程 | 第二角色用户 21:47 被外部操作移出项目,重邀被权限拒 | 恢复 88af46e8 在项目 5b9ba156 的成员身份,或提供稳定测试账号 | `bash .scratch/check/J1/journey.sh` |
| 全量单测 | 远程共享测试库(120.27.217.194/aicoding_test)锁竞争:外部机器活跃连接 + 残留连接(NAT 吞 FIN 未回收)持锁 | ①服务端 KILL 214742-45(需 DBA 权限);②与外部 checker 协调静默窗口;③conn 待超时回收 | 全量 pytest(串行,锁检干净后) |

## 环境发现(测试基建建议)

1. **登录互踢**:每次登录 token_version+1,同账号并行登录会踢掉旧 token——脚本/用例并行化时改用 JWT 自签或独立账号
2. **共享远程测试库无互斥**:并发 pytest 出现 1213 死锁假失败(失败集合漂移、重跑即绿)——建议 CI 加静默窗口或库级互斥
3. 抽查子代理曾误用仓库根 `.scratch/` 路径导致「留痕缺失」误报,实际凭据齐备(R1–R5/R7 的 ui-check.md 与 result.md 均在 `docs/{需求}/.scratch/` 下)

## 凭证纪律

CHECK_BASE_URL/CHECK_TOKEN/CHECK_TOKEN2 全程环境变量注入,凭证与 token 未写入任何脚本、报表或文档(本文档以占位标注)。
