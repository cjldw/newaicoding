# R1:关联用户字段(创建+存储)

- **状态**:⬜ | **依赖**:无 | **模块**:M1+M2

## PRD 引用(PRD-A R1)

创建对话框新增「关联用户」多选下拉(选项=项目成员,含创建者);非必填默认 [];后端校验非成员静默剔除;存储 JSON 列。

## 接口契约

- **迁移**:requirements 加 `related_user_ids JSON NULL`(对齐 knowledge_entry.tags 先例)→ DEPLOY.md
- **创建**:POST /api/requirements schema(Create,requirements.py:70-85)加 related_user_ids:list[str]|None;service create_requirement 在 :123 构造前查 project_members **静默剔除**非成员 id + 去重
- **响应**:RequirementDetailData(schema :69-86)加 related_user_ids

## 数据模型变更

- ALTER TABLE requirements ADD COLUMN related_user_ids JSON NULL COMMENT '关联用户ID列表' AFTER priority;存量行 NULL=空 → SQL 进 DEPLOY.md

## 实现要点

- 前端创建对话框(RequirementList.tsx:208-298 formData)加「关联用户」多选下拉:数据源 useProjectMembers(:304),搜索定位,已选 chips 可移除,头像+昵称+角色展示(参照成员邀请候选行样式)
- 字段定义/交互规则:照 PRD-A R1 表格原样执行
- 调研顺带修复:api/requirements.py 顶部补 ErrCode import(:124/:227 潜伏 NameError)

## 完成判据

- [ ] 创建带 2 名成员关联保存,详情返回 related_user_ids 2 项
- [ ] 不选=空数组创建成功
- [ ] 传非成员 id 被静默剔除(异常)
- [ ] 下拉可搜索、chips 可移除、头像昵称角色展示正确
- [ ] ErrCode NameError 隐患消除(相关接口回归)

## 测试验证逻辑

pytest:创建含/不含关联用户、非成员剔除;浏览器:表单交互走查。

## 实现进度

(rd-dev 维护)
