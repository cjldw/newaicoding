# R7:工作台「与我相关」口径升级

- **状态**:✅(提交 486f184;审计通过,P2 文案已修/5 P3 留痕) | **依赖**:R1(关联用户字段)+R5(交付时间) | **模块**:M1 后端+M2 前端

## PRD 引用

用户指令(2026-09-26):首页登录后展示与登录用户相关的任务需求。现有工作台(R21)四维卡+最近列表为 created_by=me 口径,本点升级为「与我相关」;**布局骨架保留,不做按角色分叉**(owner/超管全局视角走四维管理页)。

## 接口契约

- **扩展** GET /api/dashboard/summary(R21 聚合接口,先读 api/dashboard.py 现实现):四维统计与最近列表查询口径 created_by=me → **(JSON_CONTAINS(related_user_ids, me) ∪ created_by=me)**;任务类加需求传导(req_id IN 我相关的需求)并去重
- 响应结构不变(前端零破坏);recent 列表行加 delivery_date/related 透传字段(前端徽章用)

## 实现要点

- 后端:四维查询抽公共口径函数;JSON_CONTAINS(related_user_ids, '"{uid}"');任务传导查询先查我相关需求 id 集(≤50 成员×需求量可控,IN 查询)
- 前端 Dashboard.tsx:卡内行加状态徽章/交付时间徽章(复用 R5 的逾期判定);列表排序改「行动优先」:已逾期 > 明天截止 > 运行中/待办状态 > 最近更新;空态引导保留
- 布局骨架(四卡+四列表)不动

## 完成判据

- [ ] 我是某需求关联用户(非创建者)→ 该需求出现在首页需求卡统计与最近列表
- [ ] 我关联需求的开发任务出现在任务卡(传导口径,去重)
- [ ] 逾期需求排最前且带红标;明天截止黄标
- [ ] 原创建者视角回归:仅自己创建的旧数据照常出现
- [ ] tsc 零错误;pytest 口径单测

## 测试验证逻辑

pytest:四维口径(关联用户命中/创建者命中/传导去重/他人数据不出现);浏览器:双身份走查(A 关联 B 的需求,B 首页可见)。

## 实现进度

- [x] 后端:_mine_requirement_condition 统一口径(created_by=me OR JSON_CONTAINS(related_user_ids),NULL 安全双保险);任务维先查我相关需求 id 集,OR 单查询天然去重;recent 行透传 delivery_date
- [x] 前端:交付徽章(已逾期红 / 明天截止黄,GMT+8 与 R5 同约定,终态豁免)+ running pulse dot;行动优先排序(逾期 > 明天截止 > 运行中/待办 > 最近更新);布局骨架零改动零新视觉
- [x] P2:页头副标题/文件头注释旧「created_by=me」文案同步新口径(L3/L161)
- [x] QA 8 用例 + 回归 30 全绿;审计通过(P2 提交前已修,5 条 P3 留痕不改码)
