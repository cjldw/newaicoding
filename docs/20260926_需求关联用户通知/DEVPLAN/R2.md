# R2:关联用户详情可编辑

- **状态**:⬜ | **依赖**:R1 | **模块**:M1+M2

## PRD 引用(PRD-A R2)

详情页信息区查看/编辑关联用户;编辑权限沿用需求编辑口径(editor+,PATCH :109-137);空名单保存=清空(合法)。

## 接口契约

- PATCH /api/requirements/{req_id}(Update schema :21-27)加 related_user_ids;service :126-135 逐字段赋值处加同 R1 的剔除非成员+去重逻辑(抽公共函数与 R1 共用)
- 鉴权不变(:121 editor+);viewer 只读

## 实现要点

- 详情页 RequirementDetail.tsx 基本信息卡(:260-269 grid)加「关联用户」行:头像 chips 展示;编辑按钮(有权限时)开多选编辑态(复用 R1 下拉组件);保存走现有 useUpdateRequirement
- 空名单=清空合法;保存后详情刷新

## 完成判据

- [ ] editor 增删关联用户保存生效,详情刷新正确
- [ ] viewer 无编辑入口(异常)
- [ ] 清空保存后为空数组
- [ ] 非成员 id 提交被剔除(异常)

## 测试验证逻辑

pytest:PATCH 三态(增/删/清空)+viewer 403;浏览器:编辑态走查。

## 实现进度

- [x] PATCH 接 related_user_ids(传了才改,_filter_related_members 复用)
- [x] 详情「关联用户」行+编辑 Dialog(共用 RelatedUserSelect)
- [x] project_id 最小透出
- [x] QA 6/6 + 回归 23 + 审计通过
