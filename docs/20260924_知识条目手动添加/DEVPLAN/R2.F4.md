# R2.F4:知识库预览渲染增强 + 编辑保存 500 修复(BUG-KB-004)

- **状态**:✅(verified)
- **根因**(分析实证):阅读页布局已符合左树/右预览(260px/860px);真缺口=① KnowledgeBaseView 本地 renderMarkdown 缺链接/有序列表包裹(编号串号)/引用/表格;② 顺带发现 kb_service.update_doc flush 后读 updated_at 触发 MissingGreenlet → **编辑保存恒 500**

## 实现要点

1. KnowledgeBaseView.tsx 本地 renderMarkdown 扩展:行内链接 `[text](url)`(安全:仅 http/https/相对路径)、有序列表(`<ol>` 包裹,修编号串号)、引用块(`> `)、表格( |-| 分隔);保持转义优先,不引外部库;md 文件继续走渲染(现状已如此),非 md 文件维持原文展示
2. backend kb_service.update_doc:flush 后取 updated_at 改 `await db.flush()` 后 refresh 或改用返回值/onflush=COMMIT 口径(照仓库其他 service 的写法);修后编辑保存 200

## 测试验证逻辑

1. pytest:kb update_doc 保存 200 且 updated_at 变化(Red 先行:现恒 500)
2. 浏览器:预览页 md 含链接/有序列表/引用/表格的文档渲染正确(测试库已建,见 fix-analysis 证据);编辑保存成功
3. tsc 零错误
