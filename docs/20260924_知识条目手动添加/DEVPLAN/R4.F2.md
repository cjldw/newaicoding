# R4.F2:知识条目页双 Tab(项目知识库/平台知识库)(BUG-KB-003)

- **状态**:✅(verified)
- **根因**:KnowledgeBase.tsx:297 `isProjectScope &&` 门控 Tab,activeQ(:139)平台 scope 恒 platformQ

## 实现要点

仅改 KnowledgeBase.tsx(40-60 行,零后端):
1. 去 Tab 门控,/knowledge 也渲染「项目知识库 | 平台知识库」双 Tab
2. 平台 scope 增 `scopePid` 状态;「项目知识库」Tab 内工具栏左侧加项目 Select(复用 :171 projectOptions);projectQ 的 pid 取 `projectId ?? scopePid`,hook `enabled:!!pid` 兜底未选时不请求,未选显示空态「请选择项目」
3. 默认 Tab:平台 scope 默认「平台知识库」,项目 scope 维持「项目知识库」
4. 新建条目/详情跳转逻辑按 activeTab 对应 scope 走(平台 Tab 建条目仍走归属项目选择)

## 测试验证逻辑

1. /knowledge:双 Tab 可切;项目 Tab 选项目后列表出该项目条目;平台 Tab 显示平台条目
2. 项目页 /projects/{pid}/knowledge:行为与修复前一致
3. 创建入口按 Tab 语义工作(平台 Tab 建=选归属项目;项目 Tab 建入当前选中项目)
4. tsc 零错误;浏览器截图两 Tab
