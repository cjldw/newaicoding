# R4:列表摘要与入口修补

- **状态**:✅(审计 P1 归档页形状失配已修——前端适配后端平铺契约,拍板留痕变更记录;真机走查归 rd-test)
- **依赖**:R2(详情页路由存在后接通)
- **模块**:M1 后端 + M2 前端

## PRD 引用

列表卡片显示真实摘要(content 前 100 字纯文本);ArchivePage「查看」按钮接通详情页。

## 接口契约

**修补** 列表 brief:`knowledge_service._entry_brief`(knowledge_service.py:19-30)增加 `summary` 字段——content 纯文本截取前 100 字(去 markdown 标记简单处理:去 #/*/` 符号即可,不做完整渲染)
- 影响接口:GET /projects/{pid}/knowledge、GET /knowledge
- detail 接口同时返回 `created_by_user_id` 与 `permissions`(R3 复用)

## 实现要点(前端)

- KnowledgeBase.tsx 列表卡片:`truncate(entry.content,100)` 改读 `entry.summary`(现恒为空)
- 卡片点击 → 跳详情页(项目级/平台级按 entry.project_id 是否存在选路由)
- ArchivePage.tsx:154-158「查看」空实现 → 跳 `projects/:projectId/knowledge/:entryId`
- 摘要为空(A 型无 content)时显示类型占位文案,如「关联代码 · 3 个路径」

## 文案清单

- 摘要占位:「关联代码 · {n} 个路径」「(无内容)」

## 完成判据

- [ ] 列表卡片显示真实摘要(B 型 100 字截断)
- [ ] A 型无 content 条目显示「关联代码 · n 个路径」
- [ ] 卡片点击进详情,路由项目级/平台级正确
- [ ] ArchivePage 查看按钮跳详情成功
- [ ] 项目库/平台库列表均含 summary 字段

## 测试验证逻辑

- pytest:brief 含 summary(长文本截断 100、markdown 符号剔除)
- 浏览器:三入口(项目库列表/平台库列表/归档页)点击跳转走查

## 实现进度

- [x] _entry_brief 加 summary(markdown 剔除含链接取文字,截 100 字);A 型空串
- [x] detail 补 created_by_user_id;archive 条目补 project_id
- [x] 前端卡片读 summary + A 型占位;ArchivePage 查看接通(契约对齐后端平铺形状,P1 修复)
- [x] QA 10/10 + 回归 38/38 + tsc 零错误;轻量审计通过(P1 已修)
